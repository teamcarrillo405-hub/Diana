node_modules/
.next/
.next-*/
out/
dist/
.env
.env.local
.env*.local
*.log
.DS_Store
next-env.d.ts
tsconfig.tsbuildinfo
.vercel
.turbo
coverage/
.qa/
test-results/
playwright-report/
vendor/open-source/
external/

# Generated evidence and media outputs stay local and never enter a release candidate.
/artifacts/

# Local credentials and agent/review runtime state must never be committed.
.secrets/
.codex-review/
.ruvector/
.ruflo/
/agentdb.rvf
/agentdb.rvf.lock
/.tmp-*.json
/.tsconfig-beta-*.json

# Playwright session tokens must never land in the repo
**/auth-storage-state.json
.gstack/
.planning/qa-screenshots/

# Local agent runtimes and machine state
.agents/
.claude/
.claude-flow/
.codex/
.mcp.json
.swarm/
ruvector.db
supabase/.temp/
lib/supabase/types.ts.new
.tmp/
public/assets/candidates/landing/
.media-candidates/

# Machine-local specialist and media tooling is excluded from release candidates.
/plugins/comfyui-codex/
/tools/comfyui-mcp/
/tools/queue-seedance25-smoke-test.ps1
/tools/render-work-stays-yours-film*.mjs
/tools/work-stays-yours-sound.ffscript
/scripts/comfy-loading-background.json
/.codex-specialist-package.json
/--no-banner

# ── Phantom junk files ────────────────────────────────────────────────────
# Zero-byte files created when JS/TS/CSS fragments get executed in a shell and
# `>` redirects a token to a file (e.g. an AI/codex session pasting code).
# All patterns are root-anchored (leading /) so they can NEVER match real
# source like docs/design/. If a new fragment shape appears, add it here.
/[({][!-~]*
/,*
/*.,+
/0
/3
/design
/span
/helpedAt
/transcribe-note
/weekStart.getTime()
/a.due_at
/assignment.id
/window.clearInterval(id)

# Backend export bundles (generated)
/export/
