---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "23"
type: execute
wave: 7
depends_on: ["36-22"]
files_modified:
  - app/(app)/dashboard/today-game-plan.tsx
  - app/(app)/assignments/[id]/homework-mission.tsx
  - app/(app)/quiet-command.module.css
  - app/globals.css
  - app/(auth)/layout.tsx
  - app/onboarding/done/page.tsx
  - scripts/verify-screendesign-removal.ts
  - .vercelignore
autonomous: true
requirements: [P36-REMOVAL, P36-FIDELITY, P36-ASSETS, P36-QA]
must_haves:
  truths:
    - "No live route, source import, stylesheet, or clean compiled bundle contains the prohibited old visual composition per D-05."
    - "Every registry-owned canonical route is rejected by the removal gate if it imports PageShell, AppTopNav, TodayGamePlan, a Quiet Command presentation module, or an obsolete command selector/composition marker."
    - "Business logic, user data, state machines, scoring, and safety modules remain intact."
    - "Production assets are local and a clean build passes all repository gates."
  artifacts:
    - path: scripts/verify-screendesign-removal.ts
      provides: "Source, asset, and compiled-output removal gate"
  key_links:
    - from: app/(app)/dashboard/page.tsx
      to: app/(app)/dashboard/lobby-dashboard.tsx
      via: "sole dashboard presentation consumer"
---

<objective>
Remove the obsolete presentation only after every canonical replacement and action gate passes.

Purpose: Ensure the user cannot see Nexus/Mission Control/TodayGamePlan through source, stale CSS, or compiled output, while preserving backend capability.
Output: Deleted obsolete views/styles/copy, deterministic removal audit, clean build, and deploy-safe ignore rules.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@app/(app)/dashboard/page.tsx
@app/globals.css
@app/screendesign.css
@.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove obsolete dashboard and assignment presentation consumers</name>
  <files>app/(app)/dashboard/today-game-plan.tsx, app/(app)/assignments/[id]/homework-mission.tsx, app/(app)/quiet-command.module.css, app/(auth)/layout.tsx, app/onboarding/done/page.tsx</files>
  <action>After confirming replacement imports from Plans 05, 07, 08, and 12, delete the unreferenced old dashboard view and old Quiet Command assignment view/style module. Reconcile remaining auth/onboarding copy with canonical source language. Preserve `lib/homework-mission/subjects.ts`, assignment actions, scoring, state-machine, and database data because they are operational logic rather than the obsolete visual layer.</action>
  <verify><automated>npm run typecheck</automated></verify>
  <done>Old view files have no consumers or are deleted, new screen owners compile, and no business module or schema was removed.</done>
</task>

<task type="auto">
  <name>Task 2: Remove stale visual CSS and create a deterministic removal audit</name>
  <files>app/globals.css, scripts/verify-screendesign-removal.ts</files>
  <action>Remove the obsolete `pm` selector block and old student command composition rules after proving no live class references. Keep global accessibility, reading, theme, and new ScreenDesign rules. Create an audit script that reads all registry-owned route/state owners from `lib/screendesign/screens.ts` and fails when any canonical route imports `PageShell`, `AppTopNav`, `TodayGamePlan`, `quiet-command.module.css`, `homework-mission`, or another Quiet Command presentation owner. Also reject obsolete `.student-today-command`, `.pm-*`, dashboard metric-card markers, Nexus/Mission Control copy, generic old-shell composition markers, remote media hosts, and export CDN scripts across production source, route import graphs, stylesheets, and clean `.next` output. Allowlist only planning, docs, tests, and the audit's own literal table so analytical references cannot hide a production consumer.</action>
  <verify><automated>npx tsx scripts/verify-screendesign-removal.ts --source</automated></verify>
  <done>The source audit walks every registry-owned route, finds none of the prohibited shell/view imports, selectors, composition markers, copy, remote hosts, or export scripts, and documentation references do not create false positives.</done>
</task>

<task type="auto">
  <name>Task 3: Perform clean production build and compiled-output audit</name>
  <files>.vercelignore</files>
  <action>Ensure `.planning` and local QA source inputs are not imported by production runtime, and deploy ignore rules do not exclude required `public/screendesign` assets. Remove stale build output through the repository's safe clean-build mechanism, then run typecheck, unit tests, tone audit, lint, production build, full 47-state navigation suite, and the removal script against the new `.next` bundle. Do not treat old cached `.next` text as source after a clean rebuild.</action>
  <verify><automated>node scripts/run-verified-commands.mjs -- npm run typecheck --next npm run test:run --next npm run tone-audit --next npm run lint --next npm run build --next npx tsx scripts/verify-screendesign-removal.ts --compiled</automated></verify>
  <done>All repository gates pass from a clean build, required local assets are included, and compiled output contains no prohibited old composition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Removal audit to production bundle | Source and generated bundles are checked without exposing environment values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-45 | Tampering | removal script | medium | mitigate | Checked-in explicit scan scope, tests, source and clean compiled modes, no error-suppressing fallback. |
| T-36-46 | Denial of service | broad deletion | high | mitigate | Delete only verified unreferenced presentation files/selectors; preserve data, actions, and business modules. |
</threat_model>

<verification>
Run the complete clean static/build gate and inspect the diff to confirm presentation-only deletion.
</verification>

<success_criteria>
Old visual source and stale compiled output are gone, all 47 replacements still pass, and production builds only with local owned assets.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-23-SUMMARY.md` when done.
</output>
