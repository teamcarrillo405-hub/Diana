---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/screendesign/screens.ts
  - lib/screendesign/screens.test.ts
  - docs/design/SCREEN-MAP.md
  - scripts/run-verified-commands.mjs
  - scripts/run-verified-commands.test.mjs
autonomous: true
requirements: [P36-FIDELITY, P36-OPERATIONS, P36-QA]
must_haves:
  truths:
    - "The application has one authoritative registry containing exactly 47 canonical ScreenDesign states per D-01 and D-02."
    - "The attached dashboard is the only dashboard source and every entry has a route/state owner plus a primary action contract."
    - "Every multi-command phase gate can run fail-fast in Windows PowerShell 5.1 without shell chaining syntax."
  artifacts:
    - path: lib/screendesign/screens.ts
      provides: "Typed canonical screen and route/state registry"
    - path: lib/screendesign/screens.test.ts
      provides: "Count, uniqueness, precedence, and ownership gates"
    - path: scripts/run-verified-commands.mjs
      provides: "Cross-platform sequential native-command runner with explicit environment controls and first-failure exit propagation"
  key_links:
    - from: lib/screendesign/screens.ts
      to: docs/design/SCREEN-MAP.md
      via: "matching source id, route/state, and operational contract"
    - from: Phase 36 multi-command automated verification blocks
      to: scripts/run-verified-commands.mjs
      via: "`--env`, `--env-copy`, and `--unset` options followed by native argv groups separated with `--next`"
---

<objective>
Create the canonical source contract that every implementation and QA plan consumes.

Purpose: Prevent the conflicting folder dashboard, missing states, and dead prototype links from drifting back into implementation.
Output: A typed 47-entry registry, executable contract tests, a reconciled human-readable screen map, and the shell-free fail-fast runner used by all later multi-command gates.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-CONTEXT.md
@.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-RESEARCH.md
@.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-SOURCE-COVERAGE.md
@docs/design/SCREEN-MAP.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the typed canonical 47-screen registry</name>
  <files>lib/screendesign/screens.ts, lib/screendesign/screens.test.ts</files>
  <behavior>
    - The registry contains exactly 47 unique screen ids and canonical sources.
    - The attached `dashboard_personalized (1).html` owns `/dashboard`; the conflicting folder dashboard is absent.
    - Every entry declares route, state selector when needed, primary action, auth class, data owner, source viewport, and visual snapshot name.
  </behavior>
  <action>Write failing tests first, then implement readonly `ScreenDesignScreen` and `SCREEN_DESIGN_SCREENS` contracts per D-01, D-02, D-08, D-10, and D-12. Encode all rows from 36-SOURCE-COVERAGE.md. Use stable ids derived from filenames, explicit source paths, and route/state selectors such as query parameters or modal ownership. Do not import or execute the HTML.</action>
  <verify><automated>npx vitest run lib/screendesign/screens.test.ts</automated></verify>
  <done>The tests prove count, uniqueness, dashboard precedence, route/state ownership, primary-action presence, and 393x852 viewport metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the production screen map with the registry</name>
  <files>docs/design/SCREEN-MAP.md</files>
  <action>Update the map to name the attached dashboard as source 6, keep all 47 rows aligned with `SCREEN_DESIGN_SCREENS`, and add the source precedence and completion rule from D-02 and D-12. Keep security substitutions explicit for public sharing, billing, and minor community activity.</action>
  <verify><automated>npx vitest run lib/screendesign/screens.test.ts</automated></verify>
  <done>A reader can trace every canonical source to one operational route/state and no conflicting dashboard remains in the map.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the PowerShell 5.1-safe fail-fast command runner</name>
  <files>scripts/run-verified-commands.mjs, scripts/run-verified-commands.test.mjs</files>
  <behavior>
    - Commands passed after `--` execute sequentially as native argv groups separated by `--next`.
    - The runner stops at the first spawn error, signal, or nonzero exit code and returns that failure without running later commands.
    - `--env KEY=VALUE`, `--env-copy TARGET=SOURCE`, and `--unset KEY` produce one explicit child environment without PowerShell-specific syntax.
    - On Windows, npm and npx resolve to their `.cmd` shims while Node and other native executables remain shell-free.
  </behavior>
  <action>Write Node built-in tests first, then implement the checked-in ESM runner with `spawnSync`, `shell: false`, inherited stdio, strict argument parsing, Windows npm/npx shim resolution, environment copy/set/unset support, and first-failure exit propagation. Reject empty command groups, a missing source environment value, and malformed assignments. Do not concatenate command strings or invoke a shell, because every verification must remain safe and executable in Windows PowerShell 5.1.</action>
  <verify><automated>node --test scripts/run-verified-commands.test.mjs</automated></verify>
  <done>Tests prove ordered success, first-failure short circuiting, exact exit propagation, environment handling, malformed-input rejection, and Windows shim resolution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Export metadata to application contract | Untrusted HTML filenames and links become typed metadata only, never executable markup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-01 | Tampering | canonical registry | medium | mitigate | Exact count, unique ids, path precedence, and route ownership are unit tested. |
| T-36-02 | Information disclosure | public route metadata | low | accept | Registry contains no secrets or student records, only public route contracts. |
</threat_model>

<verification>
Run `node --test scripts/run-verified-commands.test.mjs`, then use the runner to execute `npx vitest run lib/screendesign/screens.test.ts`, and inspect the generated 47-row map diff.
</verification>

<success_criteria>
One tested registry accounts for all 47 states, the attached dashboard wins, downstream plans can filter screens deterministically by id, and PowerShell 5.1 can execute every later multi-command gate with first-failure propagation.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-01-SUMMARY.md` when done.
</output>
