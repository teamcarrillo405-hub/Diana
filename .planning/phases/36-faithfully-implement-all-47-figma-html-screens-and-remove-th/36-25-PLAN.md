---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "25"
type: execute
wave: 4
depends_on: ["36-03", "36-04"]
files_modified:
  - components/screen-design/smart-loading.tsx
  - components/screen-design/smart-loading.test.tsx
  - app/(app)/loading.tsx
  - app/(app)/assignments/[id]/loading.tsx
  - app/(app)/notes/loading.tsx
  - app/(app)/proof/loading.tsx
  - tests/screendesign-visual.spec.ts-snapshots/smart-loading.png
autonomous: true
requirements: [P36-FIDELITY, P36-OPERATIONS, P36-QA]
must_haves:
  truths:
    - "Smart Loading appears only during real App Router suspense/request work and never creates an artificial delay."
    - "The source hierarchy is faithful at 393x852 and reduced motion replaces movement without hiding status."
    - "All bounded route loading delegates resolve to real content as soon as their Server Component completes."
  artifacts:
    - path: components/screen-design/smart-loading.tsx
      provides: "Canonical semantic loading treatment"
    - path: app/(app)/loading.tsx
      provides: "Authenticated route suspense owner"
  key_links:
    - from: app/(app)/loading.tsx
      to: components/screen-design/smart-loading.tsx
      via: "real App Router loading boundary without timeout"
---

<objective>
Implement the canonical Smart Loading screen as one bounded route-loading concern split out of Plan 36-17.

Purpose: Keep public token sharing and authenticated route loading in separate execution units while replacing old loading presentation with the real source state.
Output: Semantic loading component, four bounded suspense owners, interaction coverage, and one reviewed golden.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@components/screen-design/source-media.tsx
@components/screen-design/screen-design-viewport.tsx
@tests/screendesign-visual.spec.ts
@.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the semantic source-faithful loading component</name>
  <files>components/screen-design/smart-loading.tsx, components/screen-design/smart-loading.test.tsx, tests/screendesign-visual.spec.ts-snapshots/smart-loading.png</files>
  <behavior>
    - The component exposes an accessible status with the canonical local media and source hierarchy.
    - Reduced motion removes looping movement while preserving the status and visual composition.
    - No timer or timeout controls how long the component remains visible.
  </behavior>
  <action>Write component tests first, then implement `smart_loading.html` through the shared viewport and local media primitives. Preserve the exact 393x852 composition, accessible status semantics, and a static reduced-motion fallback. The component receives only truthful loading context and must not fabricate progress percentage or completion.</action>
  <verify><automated>npx vitest run components/screen-design/smart-loading.test.tsx</automated></verify>
  <done>The component is semantic, source-faithful, local-asset only, reduced-motion safe, and contains no artificial delay.</done>
</task>

<task type="auto">
  <name>Task 2: Delegate bounded route suspense owners and lock the golden</name>
  <files>app/(app)/loading.tsx, app/(app)/assignments/[id]/loading.tsx, app/(app)/notes/loading.tsx, app/(app)/proof/loading.tsx, tests/screendesign-visual.spec.ts-snapshots/smart-loading.png</files>
  <action>Make the authenticated segment and the three existing nested loading owners delegate to `SmartLoading` with route-appropriate truthful labels. Do not add delays or client polling. Exercise a controlled delayed Server Component request, confirm content replaces loading immediately on resolution, compare source/app at 393x852, and write the reviewed baseline.</action>
  <verify><automated>node scripts/run-verified-commands.mjs --env SCREEN_IDS=smart-loading -- npx playwright test tests/screendesign-visual.spec.ts tests/screendesign-navigation.spec.ts --project=screendesign-mobile</automated></verify>
  <done>The loading golden passes during a controlled real suspense interval, disappears on resolution, and all four bounded owners share the new presentation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Server suspense to client-visible status | Private route work may disclose only a generic truthful loading label before owner-scoped content resolves. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-49 | Information disclosure | loading labels | low | mitigate | Labels contain no record titles, profile fields, ids, or secret state. |
| T-36-50 | Denial of service | loading boundary | medium | mitigate | No artificial timeout, retry loop, or permanent client gate; Server Component resolution owns replacement. |
</threat_model>

<verification>
Run the focused component test, controlled suspense visual/action test, reduced-motion check, and typecheck.
</verification>

<success_criteria>
Smart Loading is a real, faithful, accessible suspense state and is no longer mixed with the public sharing implementation.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-25-SUMMARY.md` when done.
</output>
