---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "30"
type: execute
wave: 8
depends_on: ["36-03", "36-22", "36-23"]
files_modified:
  - lib/screendesign/review-gallery.ts
  - lib/screendesign/review-gallery.test.ts
  - lib/screendesign/release-evidence.ts
  - lib/screendesign/release-evidence.test.ts
  - app/api/build-info/route.ts
  - scripts/generate-screendesign-review-gallery.ts
  - scripts/validate-screendesign-review-gallery.ts
  - scripts/verify-phase36-preview-sha.ts
  - test-results/screendesign-review/**
autonomous: true
requirements: [P36-QA, P36-FIDELITY, P36-OPERATIONS, P36-REMOVAL]
must_haves:
  truths:
    - "One clean producer run captures exactly 47 normalized source images and exactly 47 current app images from the canonical registry at 393x852."
    - "Every canonical id has a generated review diff even when Playwright's comparison passes with zero meaningful delta, plus combined primary-action and navigation evidence."
    - "The producer writes baseline-review metadata, artifact hashes, one run id, and the current release SHA into a machine-readable exactly-47 index."
    - "Reusable exact-set, hash, receipt, and preview-SHA validators exist before the producer runs, while Plan 36-29 invokes them independently against the producer's actual output."
    - "A producer run deletes its prior output first and cannot accept manual, extra, missing, or stale evidence."
  artifacts:
    - path: scripts/generate-screendesign-review-gallery.ts
      provides: "Safe clean-run source/app/diff/action gallery producer and index writer"
    - path: lib/screendesign/review-gallery.ts
      provides: "Deterministic output-layout, capture-plan, metadata, hashing, and completeness contracts"
    - path: scripts/validate-screendesign-review-gallery.ts
      provides: "Reusable exactly-47 validator and signed validation-receipt writer"
    - path: scripts/verify-phase36-preview-sha.ts
      provides: "Local, Vercel inspection, and served SHA equality gate used later by Plan 36-24"
    - path: test-results/screendesign-review/index.json
      provides: "Release-SHA-stamped exactly-47 machine-readable review index"
  key_links:
    - from: scripts/generate-screendesign-review-gallery.ts
      to: tests/screendesign-source-capture.spec.ts, tests/screendesign-visual.spec.ts, and tests/screendesign-navigation.spec.ts
      via: "one unfiltered run-scoped invocation collects normalized source, current app, visual result, primary action, and navigation evidence for every registry id"
    - from: scripts/generate-screendesign-review-gallery.ts
      to: test-results/screendesign-review/source, app, diff, actions, and index.json
      via: "safe output cleanup, exact-set checks, Sharp review visualization for every pair, SHA-256 hashes, baseline metadata, run id, and release SHA"
    - from: test-results/screendesign-review/index.json
      to: scripts/validate-screendesign-review-gallery.ts
      via: "The same-plan reusable validator can gate producer completion; Plan 36-29 then invokes it independently against the unchanged actual output"
    - from: scripts/verify-phase36-preview-sha.ts
      to: app/api/build-info/route.ts
      via: "Plan 36-24 compares preview inspection and served identity with the local release SHA"
---

<objective>
Produce the complete 47-screen review gallery deterministically after every canonical replacement and removal gate exists, with all reusable validation tooling already present.

Purpose: Ensure final approval uses fresh source/app/diff/action evidence generated by one executable release-SHA-stamped run, never manual or stale files.
Output: Tested producer and release-evidence contracts, clean-run CLI, reusable validators, exactly 47 source/app/diff/action artifacts, baseline metadata, HTML review surface, and machine-readable index.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@lib/screendesign/screens.ts
@tests/screendesign-source-capture.spec.ts
@tests/screendesign-visual.spec.ts
@tests/screendesign-navigation.spec.ts
@.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the safe deterministic review-gallery producer contract</name>
  <files>lib/screendesign/review-gallery.ts, lib/screendesign/review-gallery.test.ts</files>
  <behavior>
    - The capture plan is derived only from the exact 47 canonical registry ids and assigns one source, app, diff, and action path to each id.
    - Output cleanup is permitted only for the resolved repository `test-results/screendesign-review` directory and rejects the repository root, `test-results`, absolute external paths, traversal, and symlink escapes.
    - A complete row includes source/app/diff/action hashes, fixture scenario, golden path/blob hash, declared tolerance and masks, Playwright comparison result, baseline review commit and reviewer name, primary-action/navigation result, producer run id, and release SHA.
    - The final index is complete only when directory contents match the registry exactly and every row shares the top-level run id and release SHA.
  </behavior>
  <action>Write tests first, then implement pure output-path, safe-clean boundary, capture-plan, SHA-256, baseline-review metadata, row assembly, and exact-set completeness helpers. Baseline metadata must identify the tracked golden's Git blob hash, last review commit, author name without email, owning implementation plan, fixture scenario, declared tolerance/masks, and current Playwright comparison result; reject untracked, dirty, or history-less goldens and require the review commit to be an ancestor of the release SHA. Keep Playwright's `toHaveScreenshot` result authoritative for pass/fail; the producer's diff image is a review visualization and must not replace or weaken the visual matcher. Normalize all relative paths to forward-slash repository paths and make index ordering follow the canonical registry.</action>
  <verify><automated>npx vitest run lib/screendesign/review-gallery.test.ts</automated></verify>
  <done>Tests reject unsafe cleanup, missing/extra/duplicate evidence, changed hashes, inconsistent run ids or SHAs, absent passing diffs, incomplete baseline metadata, and noncanonical ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create reusable release-evidence and preview-identity validators</name>
  <files>lib/screendesign/release-evidence.ts, lib/screendesign/release-evidence.test.ts, app/api/build-info/route.ts, scripts/validate-screendesign-review-gallery.ts, scripts/verify-phase36-preview-sha.ts</files>
  <behavior>
    - Gallery validation accepts only the exact 47 canonical ids and requires matching source, app, diff, action, reviewed-baseline, run-id, release-SHA, and filesystem-hash evidence.
    - Missing, extra, duplicate, skipped, unreviewed, modified, stale, or nonpassing rows fail with their screen ids.
    - A successful actual-output validation can write a receipt containing the validator version/hash, producer run id, release SHA, index hash, validation time, and complete 47-id result without changing producer artifacts.
    - Preview validation requires full normalized equality among local expected SHA, Vercel inspection SHA, and served build SHA, while build-info returns only safe deployment identity fields.
  </behavior>
  <action>Write failing tests first, then implement the pure gallery/SHA contracts, the exactly-47 CLI, validation-receipt support, the no-store build-info route, and the preview identity CLI. The gallery validator must resolve `HEAD`, recompute every artifact hash, require exact directory sets, verify tracked clean goldens and ancestor review commits, and reject a receipt request unless the actual index passes fully. The build-info response may expose only normalized `VERCEL_GIT_COMMIT_SHA` and `VERCEL_DEPLOYMENT_ID`. These reusable validators must exist before Task 3 invokes the producer; Plan 36-29 will later invoke them independently against Task 3's unchanged output.</action>
  <verify><automated>node scripts/run-verified-commands.mjs -- npx vitest run lib/screendesign/release-evidence.test.ts --next npm run typecheck</automated></verify>
  <done>Passing and corrupt fixtures prove deterministic nonzero failures, safe receipt generation, exact SHA equality, and minimal build identity before any gallery is produced.</done>
</task>

<task type="auto">
  <name>Task 3: Generate and self-check one clean exactly-47 review gallery</name>
  <files>scripts/generate-screendesign-review-gallery.ts, test-results/screendesign-review/**</files>
  <action>Implement the CLI with required `--clean`, `--expected 47`, and `--run-id`. Resolve and validate full `git rev-parse HEAD`, remove only the safe review output root, recreate a run-scoped staging tree, explicitly unset `SCREEN_IDS`, and invoke the normalized source, current app visual, and navigation suites with `SCREENDESIGN_REVIEW_DIR`, `SCREENDESIGN_REVIEW_RUN_ID`, and the release SHA. Abort on the first nonzero suite result. Require the staging sets to contain exactly 47 normalized source PNGs, 47 current app PNGs, and 47 passing primary-action/navigation JSON records. For every canonical id, use installed Sharp only to render a review difference PNG from the captured pair, including an image when the comparison passes with zero meaningful delta; do not use this visualization to decide acceptance. Read the tracked reviewed golden's blob hash and last commit/author name from Git, require a clean golden and ancestor review commit, then combine that baseline-review metadata with the Playwright result, action/navigation evidence, fixture scenario, declared tolerance/masks, source/app/diff/action hashes, run id, and release SHA into `index.json`. Also write `release.json` and an HTML gallery that displays all 47 source/app/diff rows. Remove staging before success, reject extra files, and never copy forward an existing index or artifact.</action>
  <verify><automated>node scripts/run-verified-commands.mjs --unset SCREEN_IDS --env SCREENDESIGN_REVIEW_RUN_ID=phase36-plan30 -- npx vitest run lib/screendesign/review-gallery.test.ts --next npx tsx scripts/generate-screendesign-review-gallery.ts --clean --expected 47 --run-id phase36-plan30 --next npx tsx scripts/validate-screendesign-review-gallery.ts --expected 47 --require-complete --expected-sha HEAD --run-id phase36-plan30</automated></verify>
  <done>A fresh producer run self-checks exactly 47 normalized source images, 47 current app images, 47 diff images, 47 action/navigation records, complete baseline metadata, one release SHA, one run id, and no staging or extra files; Plan 36-29 remains the independent acceptance run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Producer cleanup to repository filesystem | A release script removes and recreates only a bounded generated-evidence directory. |
| Browser evidence to approval index | Synthetic screenshots and action records become release approval artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-60 | Tampering | gallery producer/index | high | mitigate | Clean output, exact canonical sets, run id and release SHA stamping, SHA-256 hashes, and independent Plan 36-29 recomputation. |
| T-36-61 | Denial of service | output cleanup | high | mitigate | Realpath containment, fixed allowed root, traversal/symlink/root rejection, and no caller-selected delete target. |
| T-36-62 | Information disclosure | review artifacts | high | mitigate | Synthetic owner-scoped fixtures only, no environment values, credentials, production ids, or real student records. |
| T-36-63 | Spoofing | preview build identity | high | mitigate | Reusable three-way SHA validator and minimal served identity endpoint are tested before preview use. |
</threat_model>

<verification>
Run producer and release-evidence unit tests, one unfiltered clean gallery run, and the same-plan self-check with the same run id and current HEAD. Plan 36-29 performs the independent acceptance validation afterward.
</verification>

<success_criteria>
The final review surface is reproducibly generated from one clean run and contains fresh, hashed, release-SHA-stamped source/app/diff/action evidence for every canonical id.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-30-SUMMARY.md` when done.
</output>
