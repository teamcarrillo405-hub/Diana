---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "09"
type: execute
wave: 4
depends_on: ["36-03", "36-04"]
files_modified:
  - app/(app)/inbox/page.tsx
  - app/(app)/inbox/[id]/page.tsx
  - app/(app)/inbox/[id]/confirm-form.tsx
  - app/(app)/assignments/page.tsx
  - app/(app)/quick-add/page.tsx
  - app/(app)/quick-add/capture-form.tsx
  - tests/screendesign-visual.spec.ts-snapshots/inbox-triage.png
  - tests/screendesign-visual.spec.ts-snapshots/mission-board.png
  - tests/screendesign-visual.spec.ts-snapshots/quick-add.png
autonomous: true
requirements: [P36-FIDELITY, P36-OPERATIONS, P36-QA]
must_haves:
  truths:
    - "Inbox triage renders and updates real captured items."
    - "Mission Board renders real assignment lifecycle data and routes cards to detail."
    - "Quick Add validates and creates a real inbox item before navigating."
  artifacts:
    - path: app/(app)/assignments/page.tsx
      provides: "Source-faithful Mission Board"
    - path: app/(app)/quick-add/capture-form.tsx
      provides: "Operational source-faithful capture UI"
  key_links:
    - from: app/(app)/quick-add/capture-form.tsx
      to: app/(app)/quick-add/actions.ts
      via: "validated capture and post-save route"
---

<objective>
Rebuild capture and work-board flows from three canonical sources.

Purpose: Let a student capture, triage, and enter work through the exact new visual language without losing persistence.
Output: Three operational screens, primary-action coverage, and three reviewed goldens.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@app/(app)/inbox/[id]/actions.ts
@app/(app)/quick-add/actions.ts
@lib/inbox/types.ts
@lib/state-machine/assignment.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild Inbox Triage with real confirmation actions</name>
  <files>app/(app)/inbox/page.tsx, app/(app)/inbox/[id]/page.tsx, app/(app)/inbox/[id]/confirm-form.tsx, tests/screendesign-visual.spec.ts-snapshots/inbox-triage.png</files>
  <action>Match `inbox_triage.html` at the list and selected-item states while retaining owner-scoped inbox queries, classify-inbox behavior, confirmation edits, and assignment creation. Selected states, edit controls, and confirm must persist before the item leaves triage. Empty data uses an honest source-shaped state.</action>
  <verify><automated>node scripts/run-verified-commands.mjs --env SCREEN_IDS=inbox-triage -- npx playwright test tests/screendesign-visual.spec.ts tests/screendesign-navigation.spec.ts --project=screendesign-mobile</automated></verify>
  <done>The source golden passes and triage updates a real synthetic inbox row through the existing action.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the Mission Board around real assignment states</name>
  <files>app/(app)/assignments/page.tsx, tests/screendesign-visual.spec.ts-snapshots/mission-board.png</files>
  <action>Replace the visible assignment list hierarchy with `mission_board.html` per D-06. Bind filters, counts, due context, subject accents, and cards to authenticated assignments and valid lifecycle states. Cards open `/assignments/[id]`; primary add control opens `/quick-add`. Keep factual attention language amber and retain done versus submitted distinctions.</action>
  <verify><automated>node scripts/run-verified-commands.mjs --env SCREEN_IDS=mission-board -- npx playwright test tests/screendesign-visual.spec.ts tests/screendesign-navigation.spec.ts --project=screendesign-mobile</automated></verify>
  <done>The Mission Board matches source geometry and every rendered card/action has a real destination.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild Quick Add and prove capture persistence</name>
  <files>app/(app)/quick-add/page.tsx, app/(app)/quick-add/capture-form.tsx, tests/screendesign-visual.spec.ts-snapshots/quick-add.png</files>
  <action>Match `quick_add.html` for text/voice/photo modes, local mascot imagery, selected states, and action hierarchy. Reuse existing validation, offline queue, media handling, and capture action. Success must create a real item and navigate to its triage state; cancel returns to the prior work route.</action>
  <verify><automated>node scripts/run-verified-commands.mjs --env SCREEN_IDS=quick-add -- npx playwright test tests/screendesign-visual.spec.ts tests/screendesign-navigation.spec.ts --project=screendesign-mobile --next npm run tone-audit</automated></verify>
  <done>The exact golden passes and a synthetic capture becomes a persisted triage item with no dead buttons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Capture/triage UI to server actions | Untrusted text, media references, ids, and classification choices become owner-scoped writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-17 | Tampering | inbox and quick-add actions | high | mitigate | Preserve Zod validation, authenticated owner ids, upload validation, and RLS. |
| T-36-18 | Repudiation | AI classification | medium | mitigate | Keep confirmation step and existing interaction evidence before assignment creation. |
</threat_model>

<verification>
Run the three visual/action filters, focused inbox tests, typecheck, and tone audit.
</verification>

<success_criteria>
Capture, triage, and Mission Board screens are source-faithful and persist through existing secure actions.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-09-SUMMARY.md` when done.
</output>
