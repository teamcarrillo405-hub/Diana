---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "04"
type: execute
wave: 2
depends_on: ["36-01", "36-02"]
files_modified:
  - components/screen-design/screen-design-viewport.tsx
  - components/screen-design/source-media.tsx
  - components/screen-design/primitives.tsx
  - components/screen-design/student-bottom-nav.tsx
  - components/screen-design/screen-design.test.tsx
  - app/screendesign.css
autonomous: true
requirements: [P36-FIDELITY, P36-ASSETS, P36-OPERATIONS, P36-QA]
must_haves:
  truths:
    - "Screen-specific compositions share only low-level source primitives and are not forced through the old generic shell per D-06."
    - "The mobile canvas is source-faithful at 393x852 and safely centered or expanded at larger widths."
    - "Today, Work, Classes, Calendar, and More derive active state from the current route."
  artifacts:
    - path: components/screen-design/screen-design-viewport.tsx
      provides: "Mobile-first source canvas and safe-area behavior"
    - path: components/screen-design/student-bottom-nav.tsx
      provides: "Accessible route-derived five-item navigation"
  key_links:
    - from: components/screen-design/source-media.tsx
      to: lib/screendesign/assets.ts
      via: "typed local asset id lookup"
---

<objective>
Create the narrow reusable visual layer shared by the 47 distinct compositions.

Purpose: Match repeated source mechanics without repeating the earlier mistake of wrapping old layouts in new colors.
Output: Viewport, local media, glass/action primitives, bottom navigation, CSS, and focused component tests.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@app/screendesign.css
@components/screen-design/app-mark.tsx
@lib/navigation.ts
@lib/screendesign/assets.ts
@.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the viewport, local media, and low-level visual primitives</name>
  <files>components/screen-design/screen-design-viewport.tsx, components/screen-design/source-media.tsx, components/screen-design/primitives.tsx, app/screendesign.css</files>
  <action>Create `ScreenDesignViewport`, `SourceMedia`, `GlassPanel`, `AccentChip`, `NeonAction`, and brand-mark primitives matching the recurring 393x852 canvas, navy/pink/blue/teal tokens, glass borders, italic uppercase hierarchy, safe areas, and pressed states. `SourceMedia` must require a typed local asset id, dimensions, and alt/decorative intent. Preserve reduced motion and larger-viewport safety per D-10 and D-11. Keep content hierarchy out of these primitives so individual screens remain source-specific per D-06.</action>
  <verify><automated>npx vitest run components/screen-design/screen-design.test.tsx</automated></verify>
  <done>Primitives render semantic controls and local assets at the correct mobile geometry with no remote media or export script dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the route-derived five-item student navigation</name>
  <files>components/screen-design/student-bottom-nav.tsx, components/screen-design/screen-design.test.tsx</files>
  <behavior>
    - The five labels and hrefs are Today `/dashboard`, Work `/assignments`, Classes `/classes`, Calendar `/calendar`, and More `/settings`.
    - Exactly one destination is active for each owned route and active state is not hardcoded.
    - Keyboard focus and aria-current remain visible and reduced motion removes press animation.
  </behavior>
  <action>Write tests first, then implement a client boundary limited to pathname-derived active state. Use Next.js `Link` and accessible local icons. Map nested routes to their primary owner so detail screens highlight the correct destination, per D-08.</action>
  <verify><automated>npx vitest run components/screen-design/screen-design.test.tsx</automated></verify>
  <done>All five destinations navigate correctly and the active state follows route ownership on mobile and larger layouts.</done>
</task>

<task type="auto">
  <name>Task 3: Prove responsive and calm primitive behavior</name>
  <files>components/screen-design/screen-design.test.tsx, app/screendesign.css</files>
  <action>Add assertions for semantic buttons/links, no horizontal overflow at 393px, safe centering above 393px, focus visibility, decorative image handling, and reduced-motion fallbacks. Keep error/caution tokens amber and do not introduce guilt, streak, or timer failure states per D-11.</action>
  <verify><automated>node scripts/run-verified-commands.mjs -- npx vitest run components/screen-design/screen-design.test.tsx --next npm run tone-audit</automated></verify>
  <done>The shared layer passes component, accessibility semantics, responsive, and tone checks without encoding a generic page composition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Asset manifest to client markup | Only validated local ids become browser image paths. |
| Pathname to nav state | Untrusted URL input selects presentation state but cannot grant access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-07 | Tampering | SourceMedia | medium | mitigate | Typed manifest lookup rejects arbitrary URLs and missing ids. |
| T-36-08 | Elevation of privilege | bottom nav | low | accept | Navigation is not authorization; authenticated layout and server loaders remain authoritative. |
</threat_model>

<verification>
Run the focused component suite and tone audit, then use the Playwright harness to smoke the primitive canvas at 393x852 and a larger viewport.
</verification>

<success_criteria>
Downstream screens can reproduce their sources with local media, exact mobile framing, accessible controls, and the locked five-item nav without reusing an old page shell.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-04-SUMMARY.md` when done.
</output>
