---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "22"
type: execute
wave: 6
depends_on: ["36-05", "36-07", "36-09", "36-10", "36-11", "36-12", "36-13", "36-14", "36-15", "36-16", "36-17", "36-18", "36-19", "36-20", "36-21", "36-25"]
files_modified:
  - lib/navigation.ts
  - components/screen-design/student-bottom-nav.tsx
  - tests/screendesign-navigation.spec.ts
  - tests/screendesign-route-coverage.test.ts
autonomous: true
requirements: [P36-OPERATIONS, P36-QA, P36-FIDELITY]
must_haves:
  truths:
    - "All 47 registry entries have a reachable route/state and a passing primary-action contract per D-08 and D-12."
    - "Every authenticated source resolves to exactly one of Today, Work, Classes, Calendar, or More."
    - "No visible primary control is a prototype-only dead interaction."
  artifacts:
    - path: tests/screendesign-navigation.spec.ts
      provides: "Full 47-state primary-action E2E gate"
    - path: tests/screendesign-route-coverage.test.ts
      provides: "Static route/state and nav ownership coverage"
  key_links:
    - from: lib/screendesign/screens.ts
      to: lib/navigation.ts
      via: "route/state ownership and five-destination mapping"
---

<objective>
Close the full navigation and primary-interaction graph after all screen clusters exist.

Purpose: Prove that visual fidelity is an operational application, not a linked screenshot gallery.
Output: Static route coverage, full E2E primary-action matrix, and reconciled five-nav ownership.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@lib/screendesign/screens.ts
@lib/navigation.ts
@components/screen-design/student-bottom-nav.tsx
@tests/screendesign-navigation.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove route/state and five-destination ownership for all 47 entries</name>
  <files>tests/screendesign-route-coverage.test.ts, lib/navigation.ts, components/screen-design/student-bottom-nav.tsx</files>
  <behavior>
    - Every screen registry route resolves to an existing App Router page or declared state owner.
    - Every authenticated route maps to exactly one primary destination.
    - Today/Work/Classes/Calendar/More hrefs are fixed and nested routes activate the correct owner.
  </behavior>
  <action>Write the static coverage test first, then reconcile `lib/navigation.ts` and the source bottom nav. Treat modal/query states as owned by their route rather than duplicate destinations. Do not add a new primary tab or router abstraction.</action>
  <verify><automated>npx vitest run tests/screendesign-route-coverage.test.ts</automated></verify>
  <done>All 47 entries pass route existence, state declaration, and exactly-one-primary-owner checks.</done>
</task>

<task type="auto">
  <name>Task 2: Execute every declared primary action and back path</name>
  <files>tests/screendesign-navigation.spec.ts</files>
  <action>Expand the data-driven suite to run all 47 screen ids against deterministic populated/empty fixtures. For navigation actions assert the final route/state; for mutations assert the server-visible persisted result; for guarded capability assert the honest unavailable state; for public share assert exact token scoping. Include browser back for sources that represent a back control and verify the five-item nav from each hub.</action>
  <verify><automated>node scripts/run-verified-commands.mjs --unset SCREEN_IDS -- npx playwright test tests/screendesign-navigation.spec.ts --project=screendesign-mobile</automated></verify>
  <done>Every registry primary action, five-nav destination, and represented back path passes with no skipped screen ids.</done>
</task>

<task type="auto">
  <name>Task 3: Prove visible-control reachability and calm failure behavior</name>
  <files>tests/screendesign-navigation.spec.ts, tests/screendesign-route-coverage.test.ts</files>
  <action>Add an accessibility-based inventory of visible links/buttons on each state. Require each primary control to navigate, open a real dialog/state, submit a validated action, or be disabled with an explicit truthful reason. Exercise unauthenticated, missing data, blocked AI, unavailable billing, and expired share cases with calm amber behavior.</action>
  <verify><automated>node scripts/run-verified-commands.mjs --unset SCREEN_IDS -- npx playwright test tests/screendesign-navigation.spec.ts --project=screendesign-mobile --next npm run tone-audit</automated></verify>
  <done>No visible primary control is dead, and guarded failures remain truthful, accessible, and calm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Navigation/action matrix to protected routes | Tests exercise auth, mutation, AI, billing, and public-token boundaries end to end. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-43 | Elevation of privilege | navigation matrix | high | mitigate | Negative fixtures prove UI links do not bypass destination auth/ownership/policy checks. |
| T-36-44 | Repudiation | mutation coverage | medium | mitigate | Assert persisted state and existing audit/authorship evidence, not click completion alone. |
</threat_model>

<verification>
Run static route coverage and the complete 47-state Playwright action suite with no `SCREEN_IDS` filter.
</verification>

<success_criteria>
All 47 states are reachable, owned by the correct primary destination, and operational through real routes/actions.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-22-SUMMARY.md` when done.
</output>
