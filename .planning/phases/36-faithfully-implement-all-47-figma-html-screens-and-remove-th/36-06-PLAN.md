---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "06"
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - supabase/migrations/20260715050000_screendesign_onboarding_preferences.sql
  - lib/supabase/types.ts
  - lib/onboarding/screendesign.ts
  - lib/onboarding/screendesign.test.ts
  - app/onboarding/actions.ts
autonomous: true
requirements: [P36-ONBOARDING, P36-OPERATIONS]
must_haves:
  truths:
    - "Challenge and schedule choices persist in fields that match their meanings."
    - "Completing the source flow preserves every existing diagnosis, accommodation, school, interest, and accessibility value."
    - "Plan 36-26 applies this migration to the Supabase project used by preview, verifies the live schema, and proves an owner-scoped persistence round trip before Plan 36-07 executes."
  artifacts:
    - path: supabase/migrations/20260715050000_screendesign_onboarding_preferences.sql
      provides: "Nullable constrained learning_hurdle and study_schedule_preference fields"
    - path: lib/onboarding/screendesign.ts
      provides: "Validated answer ids and update serializer"
  key_links:
    - from: app/onboarding/actions.ts
      to: profiles
      via: "authenticated validated update and onboarded_at completion"
    - from: supabase/migrations/20260715050000_screendesign_onboarding_preferences.sql
      to: scripts/verify-phase36-onboarding-schema.mjs
      via: "Plan 36-26 linked migration apply and live information-schema verification"
---

<objective>
Resolve onboarding persistence safely before reproducing the attached screens.

Purpose: Avoid coercing new answers into unrelated profile fields or deleting existing personalization.
Output: Forward-only schema, synced types, pure validation/serialization, and authenticated completion action.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@app/onboarding/actions.ts
@lib/supabase/types.ts
@supabase/migrations/0005_personalization_and_accessibility.sql
@.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add semantically correct nullable onboarding fields</name>
  <files>supabase/migrations/20260715050000_screendesign_onboarding_preferences.sql, lib/supabase/types.ts</files>
  <action>Add nullable `profiles.learning_hurdle` constrained to `time_management`, `exam_stress`, `complex_concepts`, or `staying_consistent`, and nullable `profiles.study_schedule_preference` constrained to `morning`, `after_practice`, or `late_night`. Keep existing rows and fields untouched. Add owner-only behavior through the existing profiles RLS rather than a new policy, and sync Row/Insert/Update TypeScript definitions.</action>
  <verify><automated>npm run typecheck</automated></verify>
  <done>The migration is forward-only, existing profiles remain valid, generated-style types expose both nullable fields, and the migration is ready for the mandatory linked apply in Plan 36-26.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate and serialize source onboarding answers</name>
  <files>lib/onboarding/screendesign.ts, lib/onboarding/screendesign.test.ts</files>
  <behavior>
    - Only the four hurdle ids and three schedule ids are accepted.
    - The update object contains the two selected fields and `onboarded_at`, but no unrelated profile key.
    - Invalid or missing selections return calm field errors without completing onboarding.
  </behavior>
  <action>Write tests first, then export Zod-backed answer constants, types, and a pure `buildScreenDesignOnboardingUpdate` helper. Do not map `exam_stress` to diagnoses or map time-of-day choices to timezone. This is the safest repo-consistent resolution of the research questions and respects D-07.</action>
  <verify><automated>npx vitest run lib/onboarding/screendesign.test.ts</automated></verify>
  <done>Serialization is deterministic, rejects unknown ids, and cannot overwrite legacy onboarding fields.</done>
</task>

<task type="auto">
  <name>Task 3: Add authenticated completion action</name>
  <files>app/onboarding/actions.ts</files>
  <action>Add `completeScreenDesignOnboarding` using the server Supabase client, authenticated user id, pure validator, and owner-scoped profile update. Update only the two new fields plus `onboarded_at`, preserve skip/null semantics for all other personalization, and return a calm discriminated result. Reuse the existing onboarding redirect behavior after success.</action>
  <verify><automated>node scripts/run-verified-commands.mjs -- npx vitest run lib/onboarding/screendesign.test.ts --next npm run typecheck</automated></verify>
  <done>A valid authenticated completion persists both answers and onboarding time; invalid or unauthenticated calls cannot mutate a profile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Onboarding client to server action | Untrusted answer ids cross into an authenticated profile mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-11 | Tampering | onboarding action | high | mitigate | Zod allowlists, authenticated user id, and owner-scoped update. |
| T-36-12 | Information disclosure | profile fields | medium | mitigate | No service role or cross-user reads; fields remain under existing profile RLS. |
</threat_model>

<verification>
Run the focused unit suite, TypeScript check, migration diff inspection, and a linked dry-run or schema drift check before apply.
</verification>

<success_criteria>
The two new source questions persist honestly without weakening RLS or overwriting prior onboarding data.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-06-SUMMARY.md` when done.
</output>
