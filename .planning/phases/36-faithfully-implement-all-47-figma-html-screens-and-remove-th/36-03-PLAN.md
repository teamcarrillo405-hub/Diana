---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "03"
type: execute
wave: 3
depends_on: ["36-01", "36-02", "36-27", "36-28"]
files_modified:
  - playwright.config.ts
  - tests/fixtures/screendesign.ts
  - tests/screendesign-source-capture.spec.ts
  - tests/screendesign-visual.spec.ts
  - tests/screendesign-navigation.spec.ts
autonomous: true
requirements: [P36-QA, P36-FIDELITY, P36-OPERATIONS]
must_haves:
  truths:
    - "Every canonical source and application state can be captured at exactly 393x852 in the same Windows Chromium environment per D-10."
    - "Visual tests use the typed 47-screen fixture scenarios and owner-scoped synthetic seed from Plan 36-27, and can run one screen id or the full set."
    - "Source captures are normalized through the local 28-entry media manifest, contain no export scripts, repair the attached dashboard CSS, and make zero remote requests."
  artifacts:
    - path: playwright.config.ts
      provides: "Controlled mobile, responsive, and source projects"
    - path: tests/screendesign-visual.spec.ts
      provides: "Data-driven reviewed golden comparisons"
  key_links:
    - from: tests/screendesign-visual.spec.ts
      to: lib/screendesign/screens.ts
      via: "screen id, route/state, viewport, and snapshot metadata"
    - from: tests/fixtures/screendesign.ts
      to: lib/qa/screendesign-fixtures.ts
      via: "typed per-screen scenario id, owner-scoped records, guarded state, and deterministic copy"
    - from: tests/screendesign-source-capture.spec.ts
      to: tests/helpers/normalize-screendesign-source.ts
      via: "script stripping, 28 remote-media rewrites, dashboard CSS repair, and remote-request blocking"
    - from: tests/screendesign-source-capture.spec.ts, tests/screendesign-visual.spec.ts, and tests/screendesign-navigation.spec.ts
      to: scripts/generate-screendesign-review-gallery.ts
      via: "review mode emits one normalized source image, one current app image, and one primary-action/navigation evidence record per canonical id into the producer's run-scoped staging directory"
---

<objective>
Establish the exact visual and interaction evidence contract before broad conversion.

Purpose: Make D-12 enforceable and prevent route-only or subjective completion claims.
Output: Deterministic auth fixture, source capture, 393x852 golden suite, and primary-action suite.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@lib/screendesign/screens.ts
@lib/qa/grayson-demo.ts
@app/api/qa/anonymous-session/route.ts
@tests/responsive-qa.spec.ts
@tests/a11y.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure controlled ScreenDesign test projects and auth fixture</name>
  <files>playwright.config.ts, tests/fixtures/screendesign.ts</files>
  <action>Configure a Chromium `screendesign-mobile` project fixed at 393x852, a `screendesign-source` project using the same browser/font environment, and larger responsive projects. Consume `SCREEN_DESIGN_FIXTURE_SCENARIOS` and the owner-scoped seed API from Plan 36-27 to create or reset the complete real-record graph and authenticated storage state for the selected screen ids. Freeze the clock, disable animations for snapshots, wait for local images/fonts, and scrub generated ids without using real student credentials or production records. Fail setup when a registry screen lacks a typed populated, empty, blocked, unavailable, expired, or other declared scenario needed by its tests.</action>
  <verify><automated>npx playwright test tests/screendesign-source-capture.spec.ts --project=screendesign-source --list</automated></verify>
  <done>Playwright lists the controlled projects, and every registry entry resolves to a typed owner-scoped scenario and reusable authenticated state from synthetic data only.</done>
</task>

<task type="auto">
  <name>Task 2: Create source, golden, and primary-action data-driven suites</name>
  <files>tests/screendesign-source-capture.spec.ts, tests/screendesign-visual.spec.ts, tests/screendesign-navigation.spec.ts</files>
  <action>Read the typed registry to capture every canonical HTML source and matching app route/state. Pass every source through `normalizeScreenDesignSource` from Plan 36-28: remove Tailwind/Iconify and all other export scripts, remove inline event handlers, rewrite all 24 ScreenDesign and four DiceBear URLs through `public/screendesign/manifest.json`, and repair the malformed attached-dashboard stadium declaration before navigation. Abort any source capture request whose host is not the isolated source origin or local asset origin, and assert the browser observed zero remote requests. Support `SCREEN_IDS` filtering. The visual suite must use `toHaveScreenshot` at 393x852 with stable screenshot names; the navigation suite must execute each declared primary action and verify its destination or persisted state. Add responsive overflow/focus checks and source/app console-error collection. When `SCREENDESIGN_REVIEW_DIR` and `SCREENDESIGN_REVIEW_RUN_ID` are supplied by Plan 36-30, emit exactly one normalized source PNG, one current app PNG even when comparison passes, and one action/navigation JSON record per executed canonical id into the run-scoped staging directories. The harness must not create the gallery index or omit passing-image evidence. Do not silently update goldens; baseline writes require the explicit update flag and final review in Plan 24.</action>
  <verify><automated>node scripts/run-verified-commands.mjs --env SCREEN_IDS=dashboard-personalized -- npx playwright test tests/screendesign-source-capture.spec.ts --project=screendesign-source --next npx vitest run tests/screendesign-source-normalization.test.ts</automated></verify>
  <done>The repaired attached dashboard is captured at 393x852 with all 28 media references local, no export scripts, zero remote requests, and the registry enumerates all 47 visual/action tests without duplicate snapshot names.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test fixture to Supabase | Synthetic authenticated writes must remain isolated and owner-scoped. |
| Local HTML to browser | Export scripts and remote runtime dependencies must not gain application privileges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-05 | Information disclosure | golden screenshots | high | mitigate | Synthetic identity only, deterministic scrub, and no production storage state. |
| T-36-06 | Elevation of privilege | source capture | high | mitigate | Serve sources in an isolated origin without application cookies and never inject HTML into the app. |
</threat_model>

<verification>
List all projects and screen cases, capture the dashboard source, and prove the fixture contains no production identity or secret material.
</verification>

<success_criteria>
The harness can capture source and app states in an identical 393x852 environment and prove primary interactions independently of route existence.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-03-SUMMARY.md` when done.
</output>
