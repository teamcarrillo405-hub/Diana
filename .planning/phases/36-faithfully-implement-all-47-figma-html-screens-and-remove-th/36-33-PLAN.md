---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "33"
type: execute
wave: 11
depends_on: ["36-32"]
gap_closure: true
files_modified:
  - app/screendesign.css
  - app/design-proof/page.tsx
  - components/screen-design/screen-design-viewport.tsx
  - components/screen-design/student-bottom-nav.tsx
  - components/screen-design/responsive-proof-gallery.tsx
  - tests/screendesign-visual.spec.ts
  - tests/screendesign-responsive-contract.test.ts
  - scripts/generate-responsive-proof-gallery.ts
  - public/screendesign-proof/**
autonomous: true
requirements: [P36-FIDELITY, P36-QA, P36-OPERATIONS]
must_haves:
  truths:
    - "Every canonical screen keeps its approved 393x852 mobile composition."
    - "Every canonical screen uses a full desktop canvas at 1440x1000 instead of centering the 393px phone viewport."
    - "Authenticated desktop screens use a left navigation rail derived from the same five mobile destinations."
    - "Screen families use wider reading areas, multi-column layouts, and desktop-sized work surfaces without horizontal overflow."
    - "One public proof route lets the user select any of the 47 canonical screens and see mobile and desktop evidence together."
    - "Nexus, Mission Control, command-deck, and game-plan implementation language remains absent."
  artifacts:
    - path: app/screendesign.css
      provides: "Shared and screen-family desktop composition rules at 1024px and 1440px"
    - path: app/design-proof/page.tsx
      provides: "Public side-by-side responsive proof route"
    - path: scripts/generate-responsive-proof-gallery.ts
      provides: "Deterministic exactly-47 mobile and desktop proof producer"
    - path: public/screendesign-proof/manifest.json
      provides: "Exactly-47 responsive proof manifest"
  key_links:
    - from: components/screen-design/screen-design-viewport.tsx
      to: app/screendesign.css
      via: "data-screen-design-responsive marks every canonical viewport for the shared desktop contract"
    - from: tests/screendesign-visual.spec.ts
      to: public/screendesign-proof
      via: "the same synthetic fixture scenario is captured at 393x852 and 1440x1000"
    - from: app/design-proof/page.tsx
      to: public/screendesign-proof/manifest.json
      via: "canonical registry order drives the proof selector and paired images"
---

<objective>
Add a first-class desktop implementation for all 47 canonical ScreenDesign screens while retaining the approved mobile implementation and making both formats reviewable together.

Purpose: Mobile fidelity alone is not a complete product. Desktop must be intentionally composed and visible during the same proofing pass.
Output: Responsive desktop shell, screen-family desktop layouts, exactly-47 paired evidence, public proof gallery, and release gates for both formats.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the shared mobile and desktop viewport contract</name>
  <files>components/screen-design/screen-design-viewport.tsx, components/screen-design/student-bottom-nav.tsx, app/screendesign.css, components/screen-design/screen-design.test.tsx, tests/screendesign-responsive-contract.test.ts</files>
  <behavior>
    - Mobile remains capped at the 393px source width below 1024px.
    - Desktop expands each ScreenDesign viewport to the browser width.
    - The five mobile bottom destinations become a persistent left rail on desktop with exactly one active destination.
    - Desktop widths do not alter mobile DOM semantics, destination ownership, or source screenshot fidelity.
  </behavior>
  <action>Add one explicit responsive marker to the shared viewport, desktop-only navigation identity, and a CSS breakpoint contract. Preserve every mobile selector and interaction. Add focused unit and CSS contract tests before browser verification.</action>
  <verify><automated>npx vitest run components/screen-design/screen-design.test.tsx tests/screendesign-responsive-contract.test.ts</automated></verify>
  <done>The shared viewport and navigation have distinct tested mobile and desktop behavior without changing the five-route contract.</done>
</task>

<task type="auto">
  <name>Task 2: Compose every screen family for desktop</name>
  <files>app/screendesign.css, tests/screendesign-visual.spec.ts</files>
  <action>Add desktop composition rules for dashboard, onboarding, access, calendar, assignments, inbox, classes, study, writing, notes, tutor, analytics, knowledge, proof, settings, public shares, focus, goals, flashcards, submit, breakdown, and loading families. Use desktop rails, max reading widths, larger work areas, and multi-column grids. Require every canonical route to render a viewport at least 1180px wide at 1440x1000 with no horizontal overflow, local-only requests, console errors, or page errors. Keep mobile screenshot comparison authoritative and unchanged.</action>
  <verify><automated>npx playwright test tests/screendesign-visual.spec.ts --project=screendesign-mobile --workers=1; npx playwright test tests/screendesign-visual.spec.ts --project=screendesign-responsive-desktop --workers=1</automated></verify>
  <done>All 47 mobile visual baselines pass unchanged and all 47 desktop routes pass a real full-canvas responsive gate.</done>
</task>

<task type="auto">
  <name>Task 3: Generate paired proof and publish the review surface</name>
  <files>scripts/generate-responsive-proof-gallery.ts, components/screen-design/responsive-proof-gallery.tsx, app/design-proof/page.tsx, public/screendesign-proof/**</files>
  <action>Run the canonical visual fixtures once for mobile and once for desktop, require exact 47-file sets, convert the screenshots to bounded WebP assets, and write a manifest. Build a no-index public route with previous, next, and screen-select controls that shows the selected mobile and desktop images simultaneously. Use synthetic fixture data only.</action>
  <verify><automated>npx tsx scripts/generate-responsive-proof-gallery.ts --clean --expected 47; npm run build</automated></verify>
  <done>The deployed preview contains one proof route with 47 selectable mobile and desktop pairs generated from the same canonical fixtures.</done>
</task>

</tasks>

<success_criteria>
- Mobile ScreenDesign goldens remain unchanged at 47/47.
- Desktop ScreenDesign routes pass at 47/47 with full-width layouts and no horizontal overflow.
- The proof manifest contains exactly 47 ids and 94 WebP images.
- The proof route visibly shows the two formats together for every screen.
- The responsive preview is available for user proofing before Phase 36 final approval.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-33-SUMMARY.md` when done.
</output>
