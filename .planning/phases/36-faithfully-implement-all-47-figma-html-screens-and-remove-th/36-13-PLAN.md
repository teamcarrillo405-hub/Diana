---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "13"
type: execute
wave: 4
depends_on: ["36-03", "36-04"]
files_modified:
  - app/(app)/flashcards/[id]/review/review-session.tsx
  - app/(app)/study-artifacts/page.tsx
  - app/(app)/study-artifacts/[id]/page.tsx
  - tests/screendesign-visual.spec.ts-snapshots/flashcards-review.png
  - tests/screendesign-visual.spec.ts-snapshots/study-artifacts-hub.png
  - tests/screendesign-visual.spec.ts-snapshots/practice-test-session.png
autonomous: true
requirements: [P36-FIDELITY, P36-OPERATIONS, P36-QA]
must_haves:
  truths:
    - "Flashcard review uses the real FSRS card/rating flow without guilt language."
    - "Study Artifacts Hub lists and creates real artifacts through existing AI policy gates."
    - "Practice Test Session persists responses and does not invent a score."
  artifacts:
    - path: app/(app)/flashcards/[id]/review/review-session.tsx
      provides: "Canonical operational flashcard review"
    - path: app/(app)/study-artifacts/[id]/page.tsx
      provides: "Canonical practice-test artifact state"
  key_links:
    - from: app/(app)/study-artifacts/page.tsx
      to: app/(app)/study-artifacts/actions.ts
      via: "AI-gated artifact creation and authorship recording"
---

<objective>
Rebuild flashcard and study-artifact states from three canonical sources.

Purpose: Preserve proven study behavior while replacing the visible compositions completely.
Output: Operational review, artifact hub, practice session, and three reviewed goldens.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@app/(app)/flashcards/[id]/actions.ts
@app/(app)/study-artifacts/actions.ts
@lib/fsrs/fsrs.ts
@lib/study-helper/artifacts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild Flashcards Review on the real FSRS flow</name>
  <files>app/(app)/flashcards/[id]/review/review-session.tsx, tests/screendesign-visual.spec.ts-snapshots/flashcards-review.png</files>
  <action>Match `flashcards_review.html` for card face, flip state, progress, and rating controls. Keep Again/Hard/Good/Easy mapped to the published FSRS scheduler and existing review log. Preserve TTS/audio where available, keyboard operation, neutral labels, and honest completion.</action>
  <verify><automated>node scripts/run-verified-commands.mjs --env SCREEN_IDS=flashcards-review -- npx playwright test tests/screendesign-visual.spec.ts tests/screendesign-navigation.spec.ts --project=screendesign-mobile --next npx vitest run lib/fsrs/fsrs.test.ts</automated></verify>
  <done>The source golden passes and flip/rate/advance writes a real review without changing FSRS weights.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the Study Artifacts Hub</name>
  <files>app/(app)/study-artifacts/page.tsx, tests/screendesign-visual.spec.ts-snapshots/study-artifacts-hub.png</files>
  <action>Match `study_artifacts_hub.html` using real artifact rows and supported creation modes. Cards open actual artifacts; create invokes the existing server action, AI mode, token budget, safety prompt, authorship log, and task-signal seam. Empty accounts render a source-shaped honest empty state.</action>
  <verify><automated>node scripts/run-verified-commands.mjs --env SCREEN_IDS=study-artifacts-hub -- npx playwright test tests/screendesign-visual.spec.ts tests/screendesign-navigation.spec.ts --project=screendesign-mobile</automated></verify>
  <done>The hub golden passes and create/open actions operate on real artifacts with server policy enforced.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild Practice Test Session without fabricated outcomes</name>
  <files>app/(app)/study-artifacts/[id]/page.tsx, tests/screendesign-visual.spec.ts-snapshots/practice-test-session.png</files>
  <action>Render `practice_test_session.html` for practice-test artifacts while retaining token ownership, payload parsing, responses, progress, and supported completion writes. If no score is computed by the backend, show completion/evidence without a mock percentage. Back returns to the hub and finish persists before navigation.</action>
  <verify><automated>node scripts/run-verified-commands.mjs --env SCREEN_IDS=practice-test-session -- npx playwright test tests/screendesign-visual.spec.ts tests/screendesign-navigation.spec.ts --project=screendesign-mobile --next npm run tone-audit</automated></verify>
  <done>The practice golden passes, responses survive refresh, and completion never invents a result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Study UI to scheduler/artifact actions | Ratings, responses, and AI generation cross authenticated write boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-25 | Tampering | review and artifact ids | high | mitigate | Preserve owner filters, validation, FSRS rating enum, and artifact action checks. |
| T-36-26 | Repudiation | AI artifact generation | high | mitigate | Preserve authorship and interaction logs plus stored artifact payload. |
</threat_model>

<verification>
Run all three visual/action filters, FSRS tests, artifact tests, typecheck, and tone audit.
</verification>

<success_criteria>
Review and artifact states match their sources and remain backed by real scheduler, storage, and AI safety behavior.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-13-SUMMARY.md` when done.
</output>
