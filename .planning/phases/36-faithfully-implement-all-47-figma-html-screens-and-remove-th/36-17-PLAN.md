---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "17"
type: execute
wave: 4
depends_on: ["36-03", "36-04"]
files_modified:
  - app/share/[token]/page.tsx
  - app/share/[token]/parent-summary.tsx
  - app/share/[token]/teacher-snapshot.tsx
  - tests/screendesign-visual.spec.ts-snapshots/external-scout-view.png
  - tests/screendesign-visual.spec.ts-snapshots/scout-share-view.png
autonomous: true
requirements: [P36-FIDELITY, P36-OPERATIONS, P36-QA]
must_haves:
  truths:
    - "Public source states read only exact, active, unexpired share-token data."
    - "Expired/revoked links render a calm inactive state without leaking student records."
  artifacts:
    - path: app/share/[token]/page.tsx
      provides: "Canonical token-scoped external and scout share states"
  key_links:
    - from: app/share/[token]/page.tsx
      to: share_links
      via: "server-only exact token, expiry, revocation, and owner-derived lookup"
---

<objective>
Implement the two public share sources without weakening trust boundaries.

Purpose: Match public visuals while protecting minor data through the existing exact-token server boundary.
Output: Token-scoped external and scout share states, route wiring, and two goldens.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@app/share/[token]/page.tsx
@lib/sharing/types.ts
@components/screen-design/source-media.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild token-scoped External Scout portfolio state</name>
  <files>app/share/[token]/page.tsx, tests/screendesign-visual.spec.ts-snapshots/external-scout-view.png</files>
  <action>Match `external_scout_view.html` for share links whose payload/state is portfolio evidence. Retain server-only service-role use, exact token match, unrevoked/unexpired filters, and owner id derived from the matching link. Render only approved fields and local assets. Back/close and available artifact actions must not create a general student-profile path.</action>
  <verify><automated>node scripts/run-verified-commands.mjs --env SCREEN_IDS=external-scout-view -- npx playwright test tests/screendesign-visual.spec.ts tests/screendesign-navigation.spec.ts --project=screendesign-mobile</automated></verify>
  <done>The golden passes for an active token and invalid token variants reveal no student evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild Scout Share report state</name>
  <files>app/share/[token]/page.tsx, app/share/[token]/parent-summary.tsx, app/share/[token]/teacher-snapshot.tsx, tests/screendesign-visual.spec.ts-snapshots/scout-share-view.png</files>
  <action>Match `scout_share_view.html` for supported parent/teacher report token states. Use the existing safe loader and type-specific components, preserve consent and field minimization, and render expired/revoked states calmly. Print/download actions operate only on the loaded scoped report.</action>
  <verify><automated>node scripts/run-verified-commands.mjs --env SCREEN_IDS=scout-share-view -- npx playwright test tests/screendesign-visual.spec.ts tests/screendesign-navigation.spec.ts --project=screendesign-mobile</automated></verify>
  <done>The report golden passes and active, expired, and revoked token tests preserve exact scoping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public token route to private evidence | An unauthenticated token may authorize a narrowly scoped read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-33 | Elevation of privilege | share token loader | critical | mitigate | Exact token, active/expiry checks, owner-derived scope, minimal fields, server-only service role. |
| T-36-34 | Information disclosure | share screenshots | high | mitigate | Synthetic share data only and tests for invalid/revoked/expired tokens. |
</threat_model>

<verification>
Run share token security tests, both visual/action filters, and typecheck.
</verification>

<success_criteria>
Both public states match sources and expose only token-scoped evidence.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-17-SUMMARY.md` when done.
</output>
