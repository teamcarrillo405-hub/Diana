---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "31"
type: execute
wave: 9
depends_on: ["36-30"]
gap_closure: true
files_modified:
  - app/page.tsx
  - app/public-home-funnel.tsx
  - app/onboarding/screendesign-onboarding.tsx
  - app/(app)/upgrade/upgrade-screen.tsx
  - app/(auth)/signup/page.tsx
  - lib/onboarding/public-draft.ts
  - lib/onboarding/public-draft.test.ts
  - supabase/migrations/20260717090000_public_home_onboarding_metadata.sql
  - tests/screendesign-navigation.spec.ts
  - tests/public-home-funnel.spec.ts
autonomous: true
requirements: [P36-FIDELITY, P36-OPERATIONS, P36-QA]
must_haves:
  truths:
    - "The public root follows the attached ScreenDesign sequence: welcome, educational onboarding, challenge quiz, schedule quiz, social-proof paywall composition, and standard paywall composition."
    - "The two paywall layouts remain faithful to the attached source hierarchy without restoring unsupported prices, testimonials, user counts, score claims, or time-saved claims."
    - "A visitor's validated quiz selections survive the public funnel and are copied through signup metadata into the new owner profile, so completed public onboarding is not repeated after account creation."
    - "Direct signup without a public funnel draft continues to use the authenticated onboarding flow."
    - "Every forward and back transition is keyboard-operable, deterministic, and covered by browser tests at the canonical 393x852 viewport."
  artifacts:
    - path: app/public-home-funnel.tsx
      provides: "Public ScreenDesign acquisition flow containing all five attached states after welcome"
    - path: lib/onboarding/public-draft.ts
      provides: "Validated session-scoped onboarding draft serialization"
    - path: supabase/migrations/20260717090000_public_home_onboarding_metadata.sql
      provides: "Validated signup metadata projection into owner-scoped profile preferences"
    - path: tests/public-home-funnel.spec.ts
      provides: "End-to-end route, back-navigation, keyboard, and viewport proof for the public home sequence"
  key_links:
    - from: app/public-home-funnel.tsx
      to: app/onboarding/screendesign-onboarding.tsx
      via: "public completion callback reuses the exact attached onboarding compositions without invoking an authenticated server action"
    - from: app/public-home-funnel.tsx
      to: app/(app)/upgrade/upgrade-screen.tsx
      via: "public action callbacks reuse both truthful upgrade compositions in the requested order"
    - from: app/(auth)/signup/page.tsx
      to: supabase/migrations/20260717090000_public_home_onboarding_metadata.sql
      via: "validated draft values enter auth metadata and the signup trigger copies only allowed values into the owner profile"
---

<objective>
Correct the public home experience so it contains the five attached ScreenDesign HTML compositions as one operational acquisition flow.

Purpose: The public entry point currently stops at the welcome state and sends visitors directly to signup. The attached educational, quiz, schedule, social-proof paywall, and standard paywall screens must be part of the real journey before account creation.
Output: A faithful public funnel, validated draft persistence through signup, safe profile projection, and deterministic browser evidence.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@app/page.tsx
@app/onboarding/screendesign-onboarding.tsx
@app/(app)/upgrade/upgrade-screen.tsx
@app/(auth)/signup/page.tsx
@C:/Users/glcar/AppData/Local/Temp/onboarding_educational.html
@C:/Users/glcar/AppData/Local/Temp/onboarding_quiz_challenge.html
@C:/Users/glcar/AppData/Local/Temp/onboarding_quiz_schedule.html
@C:/Users/glcar/AppData/Local/Temp/paywall_social_proof.html
@C:/Users/glcar/AppData/Local/Temp/paywall_standard.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define a validated public onboarding draft and signup projection</name>
  <files>lib/onboarding/public-draft.ts, lib/onboarding/public-draft.test.ts, app/(auth)/signup/page.tsx, supabase/migrations/20260717090000_public_home_onboarding_metadata.sql</files>
  <behavior>
    - Only the canonical learning-hurdle and study-schedule enum values can be serialized or restored.
    - Malformed, partial, unknown-version, or oversized session data is rejected and cleared without blocking signup.
    - Successful signup includes valid public selections in auth metadata and clears the browser draft only after Supabase accepts the signup.
    - The profile trigger copies only allowed metadata values and sets onboarded_at only when both selections are valid.
    - Signup without both selections keeps onboarded_at null and preserves the existing authenticated onboarding redirect.
  </behavior>
  <action>Write the pure draft tests first. Implement a small versioned sessionStorage adapter that reuses the existing ScreenDesign onboarding schemas and contains no identity, credentials, or billing data. Read it only on the client signup submit path, append validated values to the existing Supabase auth metadata, and clear it only after a successful signup response. Add an idempotent migration that replaces the existing security-definer handle_new_user function while preserving every current profile field and trigger behavior. Validate metadata with explicit allowlists inside SQL before inserting learning_hurdle, study_schedule_preference, and onboarded_at. Preserve the fixed search_path and current execute grants or revocations.</action>
  <verify><automated>node scripts/run-verified-commands.mjs -- npx vitest run lib/onboarding/public-draft.test.ts --next npm run typecheck</automated></verify>
  <done>Unit tests prove fail-closed parsing and the signup path can persist valid public choices without bypassing onboarding for direct signups.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the attached compositions the operational public home sequence</name>
  <files>app/page.tsx, app/public-home-funnel.tsx, app/onboarding/screendesign-onboarding.tsx, app/(app)/upgrade/upgrade-screen.tsx, tests/public-home-funnel.spec.ts</files>
  <behavior>
    - Root begins on the existing attached welcome composition and continues through educational, challenge, and schedule states.
    - Completing schedule stores the validated draft and opens the attached social-proof paywall composition.
    - The social-proof primary action opens the attached standard paywall composition; the standard primary action opens signup.
    - Back actions return one exact state at a time without losing selected quiz answers.
    - Existing authenticated /onboarding and /upgrade behavior remains unchanged.
  </behavior>
  <action>Write a focused public-funnel browser test before implementation. Extract callback seams only where needed: ScreenDesignOnboarding must still call its authenticated completion action by default, while public mode returns validated answers to its parent; UpgradeScreen must keep its current server-resolved checkout link by default, while public mode can use explicit local next/back callbacks and labels. Build a client PublicHomeFunnel state machine and render it from app/page.tsx. Reuse the existing source-faithful compositions and CSS classes instead of recreating them. Include both attached paywall layouts in the requested order. Keep the current truthful replacements for unsupported pricing and social-proof claims.</action>
  <verify><automated>node scripts/run-verified-commands.mjs -- npx playwright test tests/public-home-funnel.spec.ts --project=chromium --workers=1 --next npm run typecheck --next npm run tone-audit</automated></verify>
  <done>The public root contains every attached composition, forward/back controls work at 393x852, selections survive navigation, and authenticated routes have no behavior regression.</done>
</task>

<task type="auto">
  <name>Task 3: Bind the correction to the canonical navigation and release gates</name>
  <files>tests/screendesign-navigation.spec.ts, tests/public-home-funnel.spec.ts</files>
  <action>Add one canonical public-home journey assertion to the existing navigation suite without changing the 47-screen registry. Run the focused public funnel, all ScreenDesign navigation, the no-update 47-screen visual suite, accessibility/responsive coverage, unit tests, typecheck, lint, tone audit, production build, and obsolete-design removal scan. Do not update visual baselines to make a mismatch pass. Record exact command counts and any unchanged known lint warnings in the summary.</action>
  <verify><automated>node scripts/run-verified-commands.mjs -- npx playwright test tests/public-home-funnel.spec.ts tests/screendesign-navigation.spec.ts --project=chromium --workers=1 --next npx playwright test tests/screendesign-visual.spec.ts --project=chromium --workers=1 --next npx playwright test tests/screendesign-a11y-responsive.spec.ts --project=chromium --workers=1 --next npm run test:run --next npm run typecheck --next npm run lint --next npm run tone-audit --next npm run build --next npm run screendesign:removal-check</automated></verify>
  <done>The corrected public sequence is included in release navigation evidence and every canonical fidelity, operational, safety, build, and Nexus-removal gate passes without baseline drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser session to auth signup metadata | Anonymous quiz choices become metadata on a newly authenticated owner. |
| Auth metadata to public profile row | A security-definer trigger copies a tightly allowlisted subset into the owner profile. |
| Public paywall controls to billing | The public sequence may navigate to signup but cannot choose a checkout URL, price, entitlement, or subscription state. |

## Security Invariants

- Browser draft data is untrusted and is validated before every read and write.
- No owner id, role, billing capability, checkout URL, credential, or private profile field is accepted from the public funnel.
- The signup trigger preserves a fixed search_path and explicit enum allowlists.
- Billing authority remains server-resolved on the authenticated upgrade route.
</threat_model>

<verification>
- The five attached Temp files remain byte-identical to the canonical source copies already registered in Phase 36.
- The public root reaches all six states including welcome at 393x852 and supports reverse navigation.
- Signup receives only valid canonical onboarding selections.
- Existing authenticated onboarding and upgrade tests remain green.
- All 47 canonical visual baselines pass without updates.
- Nexus, Mission Control, and Today's Game Plan remain physically absent.
</verification>

<success_criteria>
- The public home screen includes every attached ScreenDesign composition in an operational order.
- Quiz selections survive through account creation and prevent duplicate onboarding only when both are valid.
- Both paywall designs are included without unsupported commercial or social-proof claims.
- Full Phase 36 gates remain green and production remains untouched pending a corrected exact-preview review.
</success_criteria>
