---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "26"
type: execute
wave: 4
depends_on: ["36-03", "36-06", "36-27"]
files_modified:
  - scripts/verify-phase36-onboarding-schema.mjs
  - tests/screendesign-onboarding-persistence.spec.ts
autonomous: true
requirements: [P36-ONBOARDING, P36-OPERATIONS, P36-QA]
must_haves:
  truths:
    - "The migration from Plan 36-06 is applied to the linked Supabase project used by the Diana preview application before Plan 36-07 starts."
    - "The live profiles table exposes both nullable constrained onboarding columns and the expected check constraints."
    - "A synthetic authenticated owner can persist and reload both answers while a second owner cannot read or mutate them, and legacy profile values remain unchanged."
  artifacts:
    - path: scripts/verify-phase36-onboarding-schema.mjs
      provides: "Linked project identity, migration history, live schema, and constraint verifier"
    - path: tests/screendesign-onboarding-persistence.spec.ts
      provides: "Owner-scoped live persistence and isolation smoke"
  key_links:
    - from: supabase/migrations/20260715050000_screendesign_onboarding_preferences.sql
      to: linked Supabase preview schema
      via: "fail-fast `supabase db push --linked` and live schema dump verification"
    - from: tests/screendesign-onboarding-persistence.spec.ts
      to: app/onboarding/actions.ts
      via: "authenticated completion write, same-owner reload, and cross-owner denial"
---

<objective>
Apply and prove the onboarding schema against the Supabase environment used by the app before the source flow is implemented.

Purpose: Prevent a locally typed migration from passing planning while the preview runtime still lacks the required columns or persistence behavior.
Output: Linked migration application evidence, live schema verifier, and owner-scoped persistence smoke.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@supabase/migrations/20260715050000_screendesign_onboarding_preferences.sql
@app/onboarding/actions.ts
@lib/onboarding/screendesign.ts
@lib/qa/screendesign-fixtures.ts
@.vercel/project.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the linked migration and verify the live schema</name>
  <files>scripts/verify-phase36-onboarding-schema.mjs</files>
  <action>Create a verifier that compares the Supabase CLI linked project ref with the project ref parsed from the preview app's `NEXT_PUBLIC_SUPABASE_URL` without logging the URL or keys. Apply pending migrations with `supabase db push --linked`, require migration `20260715050000` in linked migration history, obtain a temporary linked `public` schema dump, and assert `profiles.learning_hurdle` and `profiles.study_schedule_preference` are nullable text columns with the exact allowed-value check constraints. Delete only the temporary dump after verification and never print credentials.</action>
  <verify><automated>node scripts/run-verified-commands.mjs -- npx supabase db push --linked --next npx supabase migration list --linked --next node scripts/verify-phase36-onboarding-schema.mjs --linked</automated></verify>
  <done>The linked project used by preview contains the migration, both live nullable columns, and their exact constraints before Plan 36-07 is eligible to execute.</done>
</task>

<task type="auto">
  <name>Task 2: Prove owner-scoped persistence and legacy-field preservation</name>
  <files>tests/screendesign-onboarding-persistence.spec.ts</files>
  <action>Use the Plan 36-27 QA seed to create two isolated synthetic owners with deterministic legacy profile values. As owner A, submit a valid hurdle and schedule through `completeScreenDesignOnboarding`, reload through the normal authenticated profile client, and assert both fields plus `onboarded_at` persisted while diagnoses, accommodations, school year, class count, interests, and accessibility values are byte-for-byte unchanged. As owner B, prove owner A's row cannot be selected or updated. Exercise invalid ids and assert no completion write. Clean up only records owned by the two QA identities.</action>
  <verify><automated>npx playwright test tests/screendesign-onboarding-persistence.spec.ts --project=screendesign-mobile</automated></verify>
  <done>The app's real linked backend accepts valid selections, rejects invalid/cross-owner writes, reloads both values, and preserves every legacy field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local migration tooling to linked Supabase | Schema mutation crosses into the preview application's live database. |
| Synthetic browser session to profiles RLS | Two QA identities test owner-scoped writes and denial. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-51 | Tampering | linked schema | high | mitigate | Verify project-ref equality, migration history, live column definitions, and exact constraints after fail-fast apply. |
| T-36-52 | Information disclosure | persistence smoke | high | mitigate | Synthetic owners only, key-name/ref comparison only, RLS negative test, and owner-only cleanup. |
</threat_model>

<verification>
Require linked migration push, linked history, live schema/constraint verification, valid persistence reload, legacy preservation, and cross-owner denial.
</verification>

<success_criteria>
The actual Supabase environment used by preview is ready for the four-screen onboarding route and its persistence is proven before Plan 36-07.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-26-SUMMARY.md` when done.
</output>
