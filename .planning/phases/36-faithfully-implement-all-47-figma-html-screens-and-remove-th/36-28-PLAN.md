---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "28"
type: execute
wave: 2
depends_on: ["36-01", "36-02"]
files_modified:
  - tests/helpers/normalize-screendesign-source.ts
  - tests/fixtures/screendesign-source-server.ts
  - tests/screendesign-source-normalization.test.ts
autonomous: true
requirements: [P36-QA, P36-ASSETS, P36-FIDELITY]
must_haves:
  truths:
    - "All 47 reference captures are inert normalized documents, not executable ScreenDesign exports."
    - "All 24 ScreenDesign and four DiceBear media URLs are rewritten through the checked-in local manifest before capture."
    - "The malformed attached-dashboard stadium CSS is repaired deterministically and the isolated source browser makes zero remote requests."
  artifacts:
    - path: tests/helpers/normalize-screendesign-source.ts
      provides: "Pure export-script stripping, media rewriting, and dashboard CSS repair"
    - path: tests/fixtures/screendesign-source-server.ts
      provides: "Isolated normalized-source origin and remote-request deny policy"
  key_links:
    - from: tests/helpers/normalize-screendesign-source.ts
      to: public/screendesign/manifest.json
      via: "exact original URL to local asset replacement for all 28 remote media entries"
    - from: tests/fixtures/screendesign-source-server.ts
      to: tests/screendesign-source-capture.spec.ts
      via: "normalized HTML response and zero-remote-request evidence"
---

<objective>
Normalize the untrusted exported HTML into a deterministic, local-asset-only source comparison surface.

Purpose: Ensure source/app diffs compare intended design composition rather than broken CSS, export CDN behavior, or remote asset drift.
Output: Pure normalizer, isolated source server, 47-source coverage tests, and remote-request denial.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@lib/screendesign/screens.ts
@public/screendesign/manifest.json
@C:/Users/glcar/Downloads/dashboard_personalized (1).html
@.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize every canonical HTML source deterministically</name>
  <files>tests/helpers/normalize-screendesign-source.ts, tests/screendesign-source-normalization.test.ts</files>
  <behavior>
    - Script, module, preload, remote stylesheet, and export-only runtime tags plus inline event handlers are absent from normalized output.
    - Every occurrence of the 24 ScreenDesign and four DiceBear URLs maps to the exact local manifest path, and no unmapped remote media URL remains.
    - The attached dashboard's malformed stadium declaration becomes valid CSS with separate local stadium and athlete layers while unrelated style declarations remain stable.
    - All 47 registry sources normalize successfully and the folder dashboard excluded by D-02 is never accepted as canonical.
  </behavior>
  <action>Write the canonical-source tests first, then implement pure `normalizeScreenDesignSource({ screen, html, assets })`. Parse only enough markup to remove all export scripts, external stylesheet/runtime tags, and inline handlers; rewrite media references by exact original URL through the verified 28-entry manifest; inject the existing compiled application/source-capture CSS needed to replace Tailwind CDN utility evaluation; and apply a source-id-specific repair for the malformed attached dashboard declaration. Do not evaluate source JavaScript, import HTML into the app, or permit fuzzy URL substitution.</action>
  <verify><automated>npx vitest run tests/screendesign-source-normalization.test.ts</automated></verify>
  <done>All 47 canonical inputs normalize without scripts or remote URLs, all 28 assets are local, the dashboard CSS parses, and dashboard precedence is enforced.</done>
</task>

<task type="auto">
  <name>Task 2: Serve normalized sources in an isolated zero-remote origin</name>
  <files>tests/fixtures/screendesign-source-server.ts, tests/screendesign-source-normalization.test.ts</files>
  <action>Create a test-only source server that resolves source ids exclusively through the canonical registry, returns the normalized HTML, and serves manifest assets from `public/screendesign`. Add browser/request policy hooks that abort and record any request outside the isolated source origin and local asset path. The fixture must expose request evidence so Plan 36-03 can assert zero remote requests per screen and fail on any attempted ScreenDesign, DiceBear, Tailwind CDN, Iconify CDN, font CDN, or other internet host.</action>
  <verify><automated>node scripts/run-verified-commands.mjs -- npx vitest run tests/screendesign-source-normalization.test.ts --next npm run typecheck</automated></verify>
  <done>The isolated source fixture serves all 47 normalized documents and proves zero allowed or completed remote requests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Exported HTML to source browser | Untrusted scripts, handlers, URLs, and malformed CSS must become inert deterministic reference markup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-55 | Elevation of privilege | source HTML | high | mitigate | Strip executable tags/handlers, isolate origin, deny external requests, and never share app cookies. |
| T-36-56 | Tampering | visual reference assets | high | mitigate | Exact manifest rewrites, checksums from Plan 36-02, deterministic CSS repair, and complete canonical-source tests. |
</threat_model>

<verification>
Run the 47-source normalization suite and require script-free output, exact 28-media locality, valid dashboard CSS, and zero remote requests.
</verification>

<success_criteria>
Every source comparison is deterministic, inert, locally rendered, and visually valid before the Playwright harness consumes it.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-28-SUMMARY.md` when done.
</output>
