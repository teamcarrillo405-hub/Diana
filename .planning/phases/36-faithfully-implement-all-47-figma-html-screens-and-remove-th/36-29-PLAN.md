---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "29"
type: execute
wave: 9
depends_on: ["36-30"]
files_modified:
  - test-results/screendesign-review/validation.json
autonomous: true
requirements: [P36-QA, P36-REMOVAL]
must_haves:
  truths:
    - "Plan 36-30 has already produced one clean exactly-47 gallery before this plan starts."
    - "The unchanged Plan 36-30 output is independently revalidated for exact ids, complete source/app/diff/action sets, baseline metadata, hashes, run id, and release SHA."
    - "Plan 36-24 cannot start without the successful validation receipt bound to the actual `phase36-plan30` index."
  artifacts:
    - path: test-results/screendesign-review/validation.json
      provides: "Independent validator receipt for the unchanged Plan 36-30 producer output"
  key_links:
    - from: scripts/validate-screendesign-review-gallery.ts
      to: test-results/screendesign-review/index.json
      via: "independent exact-set, hash, baseline, run-id, and release-SHA recomputation after Plan 36-30 completes"
    - from: test-results/screendesign-review/validation.json
      to: .planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-VERIFICATION.md
      via: "Plan 36-24 requires the receipt for run `phase36-plan30` before local release or human-review gates"
---

<objective>
Independently validate the actual clean exactly-47 evidence produced by Plan 36-30 without regenerating or modifying it.

Purpose: Separate evidence production from acceptance so a producer self-check cannot be the only proof used by the release gate.
Output: A deterministic validation receipt bound to the producer run id, release SHA, index hash, and exact 47-screen result.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@lib/screendesign/screens.ts
@lib/screendesign/release-evidence.ts
@scripts/validate-screendesign-review-gallery.ts
@test-results/screendesign-review/index.json
@.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run corruption and exact-set validator contracts independently</name>
  <files>test-results/screendesign-review/validation.json</files>
  <action>Run the release-evidence test suite after Plan 36-30 completes. Require fixtures for missing, extra, duplicate, skipped, unreviewed, hash-modified, stale-run, stale-SHA, dirty-golden, and non-ancestor review cases to fail. Do not edit validator or producer code in this plan and do not touch the actual gallery while running negative fixtures.</action>
  <verify><automated>npx vitest run lib/screendesign/release-evidence.test.ts</automated></verify>
  <done>The independent run proves the checked-in validator still rejects every corrupt evidence class before it examines the actual producer output.</done>
</task>

<task type="auto">
  <name>Task 2: Validate the unchanged Plan 36-30 output and write its receipt</name>
  <files>test-results/screendesign-review/validation.json</files>
  <action>Invoke the checked-in validator against the existing `phase36-plan30` index with `--expected 47`, `--require-complete`, and `--expected-sha HEAD`. Recompute exact registry ids, directory membership, every source/app/diff/action hash, baseline golden blob hash and clean state, ancestor review commit, top-level and row run ids, and release SHA. Write `validation.json` only after the complete actual output passes; include the validator version/hash, producer run id, release SHA, index hash, complete id count, and validation timestamp. Do not invoke the producer, update screenshots, clean the gallery, or copy evidence forward.</action>
  <verify><automated>npx tsx scripts/validate-screendesign-review-gallery.ts --expected 47 --require-complete --expected-sha HEAD --run-id phase36-plan30 --write-receipt test-results/screendesign-review/validation.json</automated></verify>
  <done>The actual Plan 36-30 output passes independently and has one receipt bound to its unchanged exactly-47 index, run id, hashes, and release SHA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Producer evidence to independent acceptance | Generated screenshots, diffs, action records, baseline metadata, and index claims are treated as untrusted until recomputed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-64 | Tampering | producer gallery/index | high | mitigate | Exact registry and directory sets, recomputed hashes, clean tracked goldens, ancestor reviews, and immutable run/SHA binding. |
| T-36-65 | Repudiation | validation receipt | medium | mitigate | Receipt records validator identity, index hash, run id, release SHA, count, and validation time only after a complete pass. |

</threat_model>

<verification>
Run corruption fixtures, validate the existing unmodified `phase36-plan30` gallery, and require its receipt. Confirm no producer command ran and no source/app/diff/action artifact changed.
</verification>

<success_criteria>
Plan 36-30's actual exactly-47 output has passed a later independent acceptance run, and Plan 36-24 has a concrete receipt to require before preview or human review.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-29-SUMMARY.md` when done.
</output>
