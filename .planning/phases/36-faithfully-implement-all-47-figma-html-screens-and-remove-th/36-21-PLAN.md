---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "21"
type: execute
wave: 4
depends_on: ["36-03", "36-04"]
files_modified:
  - app/(app)/upgrade/page.tsx
  - app/(app)/upgrade/upgrade-screen.tsx
  - tests/screendesign-visual.spec.ts-snapshots/paywall-standard.png
  - tests/screendesign-visual.spec.ts-snapshots/paywall-social-proof.png
autonomous: true
requirements: [P36-FIDELITY, P36-OPERATIONS, P36-QA]
must_haves:
  truths:
    - "Standard and community paywall sources render from the same truthful billing capability."
    - "Checkout appears only when the existing server resolver returns a configured URL."
    - "Unavailable billing never masquerades as a completed purchase flow."
  artifacts:
    - path: app/(app)/upgrade/upgrade-screen.tsx
      provides: "Canonical standard and community upgrade compositions"
  key_links:
    - from: app/(app)/upgrade/page.tsx
      to: lib/billing/checkout.ts
      via: "server-resolved billing capability and checkout URL"
---

<objective>
Implement both canonical upgrade states without creating fake billing behavior.

Purpose: Match the source confidence and social proof while preserving honest capability gating.
Output: Standard/community compositions, real checkout/unavailable behavior, and two goldens.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@app/(app)/upgrade/page.tsx
@lib/billing/checkout.ts
@app/api/billing/checkout/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild standard paywall with real billing gate</name>
  <files>app/(app)/upgrade/page.tsx, app/(app)/upgrade/upgrade-screen.tsx, tests/screendesign-visual.spec.ts-snapshots/paywall-standard.png</files>
  <action>Match `paywall_standard.html` at `/upgrade`, using server-loaded account/billing capability and local media. Preserve source plan hierarchy and CTA. When checkout is configured, route through the existing server endpoint/resolved URL; otherwise show the existing honest preview/settings state and never simulate purchase success.</action>
  <verify><automated>node scripts/run-verified-commands.mjs --env SCREEN_IDS=paywall-standard -- npx playwright test tests/screendesign-visual.spec.ts tests/screendesign-navigation.spec.ts --project=screendesign-mobile --next npx vitest run lib/billing/checkout.test.ts</automated></verify>
  <done>The golden passes in configured/unconfigured fixtures and CTA behavior matches the real billing capability.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild community social-proof paywall state</name>
  <files>app/(app)/upgrade/page.tsx, app/(app)/upgrade/upgrade-screen.tsx, tests/screendesign-visual.spec.ts-snapshots/paywall-social-proof.png</files>
  <action>Match `paywall_social_proof.html` at `?view=community` with local source media and truthful product/community copy. Reuse the same server billing gate and checkout endpoint as standard view. Do not invent community membership, testimonials, or purchase confirmation unsupported by product data.</action>
  <verify><automated>node scripts/run-verified-commands.mjs --env SCREEN_IDS=paywall-social-proof -- npx playwright test tests/screendesign-visual.spec.ts tests/screendesign-navigation.spec.ts --project=screendesign-mobile --next npm run tone-audit</automated></verify>
  <done>The community golden passes and checkout/unavailable behavior is identical in authority to the standard view.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Upgrade UI to billing endpoint | A client CTA may begin a server-controlled external checkout. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-41 | Spoofing | billing capability | high | mitigate | Server resolver is authoritative; client never fabricates configured state or URL. |
| T-36-42 | Tampering | checkout URL | high | mitigate | Use the existing server endpoint/allowlisted resolver, not a query-provided URL. |
</threat_model>

<verification>
Run both visual/action filters, billing tests, typecheck, and tone audit across configured and unavailable fixtures.
</verification>

<success_criteria>
Both paywall sources match and remain honest, server-gated billing surfaces.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-21-SUMMARY.md` when done.
</output>
