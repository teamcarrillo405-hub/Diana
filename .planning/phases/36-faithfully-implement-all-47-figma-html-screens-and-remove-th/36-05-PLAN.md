---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "05"
type: execute
wave: 4
depends_on: ["36-03", "36-04"]
files_modified:
  - lib/dashboard/lobby-view.ts
  - lib/dashboard/lobby-view.test.ts
  - app/(app)/dashboard/page.tsx
  - app/(app)/dashboard/lobby-dashboard.tsx
  - app/(app)/dashboard/lobby-background-layer.tsx
  - app/(app)/dashboard/player-photo-slot.tsx
  - app/(app)/dashboard/needs-attention.tsx
  - tests/screendesign-visual.spec.ts-snapshots/dashboard-personalized.png
autonomous: true
requirements: [P36-FIDELITY, P36-ASSETS, P36-OPERATIONS, P36-QA, P36-REMOVAL]
must_haves:
  truths:
    - "The authenticated landing page is the attached stadium and athlete Lobby composition per D-02 and D-03."
    - "One real next assignment and real attention categories drive the source-shaped cards."
    - "No Nexus, Mission Control, or metric-card game plan composition is rendered by `/dashboard`."
  artifacts:
    - path: app/(app)/dashboard/lobby-dashboard.tsx
      provides: "Attached source-faithful student landing composition"
    - path: lib/dashboard/lobby-view.ts
      provides: "Real loader rows to source-shaped Lobby view model"
  key_links:
    - from: app/(app)/dashboard/page.tsx
      to: lib/dashboard/lobby-view.ts
      via: "existing profile, ranked assignment, class, reminder, and attention data"
---

<objective>
Hard-replace the live student landing page with the separately attached dashboard source.

Purpose: Correct the most visible design failure first while preserving the operational dashboard loader.
Output: Tested view model, exact Lobby composition, working CTAs/nav, and reviewed 393x852 golden.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/Users/glcar/Downloads/dashboard_personalized (1).html
@app/(app)/dashboard/page.tsx
@lib/dashboard/needs-attention.ts
@lib/scoring/next-five-minutes.ts
@components/screen-design/student-bottom-nav.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adapt real dashboard data into the Lobby contract</name>
  <files>lib/dashboard/lobby-view.ts, lib/dashboard/lobby-view.test.ts, app/(app)/dashboard/page.tsx</files>
  <behavior>
    - Student display name comes from the authenticated profile with a calm fallback.
    - Next move comes from the existing ranked assignment and exposes class, title, estimate, and route.
    - Needs Attention groups factual quiz/test, overdue/open, and not-submitted work without fabricating records.
    - Empty data produces honest source-shaped empty cards and never seeds fake success.
  </behavior>
  <action>Write tests first, then create `buildLobbyDashboardView` using the existing page queries, scorer output, reminders, task signals, classes, and state-machine semantics. Keep queries in the Server Component. Preserve the 1.6x dyslexia multiplier and explicit done versus submitted distinction per D-07 and D-11.</action>
  <verify><automated>npx vitest run lib/dashboard/lobby-view.test.ts</automated></verify>
  <done>Representative populated and empty fixtures produce stable, serializable Lobby data with working hrefs and no invented content.</done>
</task>

<task type="auto">
  <name>Task 2: Render the attached stadium Lobby composition exactly</name>
  <files>app/(app)/dashboard/lobby-dashboard.tsx, app/(app)/dashboard/lobby-background-layer.tsx, app/(app)/dashboard/player-photo-slot.tsx, app/(app)/dashboard/needs-attention.tsx, app/(app)/dashboard/page.tsx</files>
  <action>Implement the attached source hierarchy per D-03: local stadium layer, separate athlete cutout, logo, `LOBBY / {name}`, notification control, `YOUR NEXT MOVE`, one teal start action, source-shaped class/title/estimate, stacked Needs Attention cards, and the five-item nav. Correct the malformed exported background declaration through local assets. Use real links and semantic controls. Remove `TodayGamePlan` from the page's render/import path and do not reuse its metric cards, copy, or layout per D-05 and D-06.</action>
  <verify><automated>node scripts/run-verified-commands.mjs --env SCREEN_IDS=dashboard-personalized -- npm run typecheck --next npx playwright test tests/screendesign-navigation.spec.ts --project=screendesign-mobile</automated></verify>
  <done>`/dashboard` renders only the Lobby source composition, start opens the real assignment, attention cards deep-link correctly, and all five nav items work.</done>
</task>

<task type="auto">
  <name>Task 3: Compare and lock the dashboard golden</name>
  <files>tests/screendesign-visual.spec.ts-snapshots/dashboard-personalized.png</files>
  <action>Capture source and app in the controlled environment, correct layout/image/font deltas until the app matches the attached file at 393x852, then write the reviewed app baseline. Verify larger widths center or safely expand without reordering the mobile hierarchy per D-10. Do not accept a color-only match.</action>
  <verify><automated>node scripts/run-verified-commands.mjs --env SCREEN_IDS=dashboard-personalized -- npx playwright test tests/screendesign-visual.spec.ts --project=screendesign-mobile</automated></verify>
  <done>The exact viewport golden passes and source/app review confirms stadium, athlete, Lobby header, one next move, attention stack, and five-nav composition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Server loader to dashboard view | Authenticated student records become a minimal serialized UI model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-09 | Information disclosure | Lobby view model | high | mitigate | Select only authenticated owner data and serialize only displayed fields. |
| T-36-10 | Tampering | start/attention hrefs | medium | mitigate | Build links from server-loaded ids; destination loaders recheck ownership. |
</threat_model>

<verification>
Run unit, type, navigation, exact golden, responsive, tone, and a11y checks for `/dashboard`.
</verification>

<success_criteria>
The student's landing page is visually the attached dashboard, operationally powered by existing data, and contains no old dashboard composition.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-05-SUMMARY.md` when done.
</output>
