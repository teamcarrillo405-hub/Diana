---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/localize-screendesign-assets.mjs
  - lib/screendesign/assets.ts
  - lib/screendesign/assets.test.ts
  - public/screendesign/manifest.json
  - public/screendesign/**
autonomous: true
requirements: [P36-ASSETS, P36-FIDELITY]
must_haves:
  truths:
    - "All 24 ScreenDesign resources and four DiceBear avatars are served from Diana-owned paths per D-09."
    - "Each local asset has a checksum, intrinsic dimensions, source URL, semantic role, and consumer list."
  artifacts:
    - path: public/screendesign/manifest.json
      provides: "28-entry local media manifest"
    - path: scripts/localize-screendesign-assets.mjs
      provides: "Deterministic GET-based acquisition and verification"
  key_links:
    - from: lib/screendesign/assets.ts
      to: public/screendesign/manifest.json
      via: "typed manifest loader and local-path lookup"
---

<objective>
Own every exported media dependency locally before screen implementation starts.

Purpose: Preserve source imagery while removing runtime tracking, availability, and CSS breakage risks from design hosts.
Output: Local optimized media, a deterministic acquisition script, typed lookup, and integrity tests.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-CONTEXT.md
@.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the canonical asset acquisition pipeline</name>
  <files>scripts/localize-screendesign-assets.mjs, public/screendesign/manifest.json, public/screendesign/**</files>
  <action>Parse the 46 canonical folder exports plus the attached dashboard override, extract unique `media.screensdesign.com` and DiceBear URLs, and fetch with ordinary GET requests. Assign descriptive stable paths under `brand`, `dashboard`, `onboarding`, `instructional`, `portfolio`, `social`, and `tutors`. Use installed Sharp only where conversion preserves alpha and focal crop. Record original URL, local path, SHA-256, MIME type, width, height, alpha intent, and every consumer screen. Repair the dashboard's malformed CSS reference by mapping the stadium and athlete layers separately. Fail on a duplicate local path, missing response body, unexpected MIME type, or count other than 24 plus four.</action>
  <verify><automated>node scripts/localize-screendesign-assets.mjs --verify</automated></verify>
  <done>The repository contains 28 verified local assets and a reproducible manifest; rerunning verify performs no network writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose safe typed asset lookup and integrity gates</name>
  <files>lib/screendesign/assets.ts, lib/screendesign/assets.test.ts</files>
  <behavior>
    - Every manifest entry resolves to an existing `/screendesign/...` file with matching checksum and dimensions.
    - No canonical asset lookup returns a remote URL.
    - Stadium, athlete, welcome background, Diana logo, mascot, tutor portraits, and four community avatars have distinct stable ids.
  </behavior>
  <action>Write tests first, then add a server-safe typed loader and `getScreenDesignAsset(id)` helper. Validate the JSON shape at module load without exposing original URLs to client rendering. Include a canonical-source scan test that proves all 28 remote dependencies have a local mapping.</action>
  <verify><automated>npx vitest run lib/screendesign/assets.test.ts</automated></verify>
  <done>Typed lookup only returns local paths and tests prove count, files, hashes, dimensions, and complete URL coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Remote design host to repository | Untrusted binary responses cross into checked-in public assets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-03 | Tampering | asset downloader | high | mitigate | Allowlisted HTTPS hosts, MIME validation, Sharp decode, exact counts, and SHA-256 manifest. |
| T-36-04 | Information disclosure | manifest | low | accept | Original public design URLs contain no credentials; manifest excludes environment and student data. |
</threat_model>

<verification>
Run the acquisition script in verify mode and the asset integrity unit test. Inspect browser network later to confirm only local media paths.
</verification>

<success_criteria>
The canonical set has 24 ScreenDesign files and four avatars under application ownership, with zero runtime design-host dependency.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-02-SUMMARY.md` when done.
</output>
