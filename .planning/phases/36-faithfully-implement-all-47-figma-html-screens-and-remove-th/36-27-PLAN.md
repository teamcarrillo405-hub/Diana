---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "27"
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - lib/qa/screendesign-fixtures.ts
  - lib/qa/screendesign-fixtures.test.ts
  - lib/qa/grayson-demo.ts
  - app/api/qa/anonymous-session/route.ts
autonomous: true
requirements: [P36-QA, P36-OPERATIONS, P36-FIDELITY]
must_haves:
  truths:
    - "Every one of the 47 registry entries declares a typed deterministic fixture scenario with the real records and guarded states its visual and action tests need."
    - "The QA seed creates records under one isolated synthetic owner and never relies on production users, static browser mocks, or cross-owner reads."
    - "Empty, blocked AI, unavailable billing/LMS, expired or revoked sharing, no-score practice, and other guarded states are explicit scenarios rather than ad hoc test branches."
  artifacts:
    - path: lib/qa/screendesign-fixtures.ts
      provides: "Typed per-screen scenario and deterministic copy contract"
    - path: lib/qa/grayson-demo.ts
      provides: "Owner-scoped real-record graph builder and cleanup contract"
  key_links:
    - from: lib/qa/screendesign-fixtures.ts
      to: lib/screendesign/screens.ts
      via: "exactly one or more declared fixture scenarios for every canonical screen id"
    - from: app/api/qa/anonymous-session/route.ts
      to: lib/qa/grayson-demo.ts
      via: "server-only seed/reset endpoint scoped to the authenticated QA owner"
---

<objective>
Define and seed the complete deterministic data contract required to test all 47 operational states.

Purpose: Ensure visual and primary-action evidence runs against real owner-scoped records and explicit guarded states instead of incomplete mocks or incidental database state.
Output: Typed scenario catalog, contract tests, owner-scoped seed implementation, and reset/cleanup route.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@lib/screendesign/screens.ts
@lib/qa/grayson-demo.ts
@app/api/qa/anonymous-session/route.ts
@.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-SOURCE-COVERAGE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define typed fixture scenarios for all 47 screen contracts</name>
  <files>lib/qa/screendesign-fixtures.ts, lib/qa/screendesign-fixtures.test.ts</files>
  <behavior>
    - Every canonical screen id has a deterministic default scenario and every guarded branch required by its plan has a named scenario.
    - A scenario declares auth class, owner id alias, fixed clock, deterministic display copy, route params/state selector, required record factories, guarded capability state, expected primary action, and expected persisted result.
    - Scenario references are type-checked against the screen registry and duplicate ids, missing screens, unsupported record kinds, or undeclared guarded states fail tests.
  </behavior>
  <action>Write the coverage tests first, then define readonly `ScreenDesignFixtureScenario`, record-factory unions, guarded-state unions, and `SCREEN_DESIGN_FIXTURE_SCENARIOS`. Include populated and honest-empty branches where the same source owner requires both. Include explicit scenarios for AI red/yellow blocks, billing unavailable/configured, LMS disconnected/error/connected, active/expired/revoked share tokens, opt-in group membership/nonmembership, no-score practice completion, onboarding invalid/valid values, and reduced motion. Use the deterministic source copy selected by the resolved research decision and never use a real student's name, credentials, or record ids.</action>
  <verify><automated>npx vitest run lib/qa/screendesign-fixtures.test.ts</automated></verify>
  <done>Tests prove complete 47-screen scenario coverage, guarded-state coverage, stable copy/clock, unique ids, and type-safe primary-action expectations.</done>
</task>

<task type="auto">
  <name>Task 2: Seed and reset the real owner-scoped QA record graph</name>
  <files>lib/qa/grayson-demo.ts, app/api/qa/anonymous-session/route.ts, lib/qa/screendesign-fixtures.test.ts</files>
  <action>Extend the existing synthetic Grayson path into `seedScreenDesignScenario` and `resetScreenDesignOwner`. Create only records required by the selected scenario across profiles, classes, assignments/checklists/signals/time logs, inbox, notes, flashcards/reviews, artifacts, mastery/grades, AP plans/attempts, notifications, integrations, portfolios/proofs, privacy/export evidence, wellness, groups/memberships, tutor preferences, AI interactions/authorship, and share tokens. Every row must carry or derive the QA owner's id; server-only elevated access may bootstrap the synthetic auth identity but must not enter client responses. Make seed/reset idempotent, return only stable aliases and storage state inputs, and prove a second synthetic owner cannot observe or mutate the first owner's records.</action>
  <verify><automated>node scripts/run-verified-commands.mjs -- npx vitest run lib/qa/screendesign-fixtures.test.ts --next npm run typecheck</automated></verify>
  <done>The QA API can deterministically create/reset every selected scenario under its owner, cross-owner isolation tests pass, and no production identity or secret is serialized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| QA seed route to Supabase | Server-only synthetic identity bootstrap and owner-scoped record creation cross database boundaries. |
| Scenario catalog to browser tests | Stable synthetic aliases and expected outcomes are serialized without credentials or service-role material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-53 | Elevation of privilege | QA seed route | high | mitigate | Existing QA gate, server-only elevated client, strict synthetic owner allowlist, no arbitrary owner id input, and owner-only returned metadata. |
| T-36-54 | Information disclosure | fixture records | high | mitigate | Synthetic copy only, cross-owner negative tests, no production reads, credentials, or secret values in scenarios or screenshots. |
</threat_model>

<verification>
Run exact registry/scenario coverage, deterministic seed/reset tests, cross-owner isolation, and TypeScript checks.
</verification>

<success_criteria>
All 47 screen tests have deterministic real records and explicit guarded states owned by isolated QA identities.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-27-SUMMARY.md` when done.
</output>
