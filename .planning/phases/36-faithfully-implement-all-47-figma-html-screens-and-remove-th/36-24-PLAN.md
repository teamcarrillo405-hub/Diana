---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "24"
type: execute
wave: 11
depends_on: ["36-23", "36-29", "36-31", "36-32"]
files_modified:
  - .planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-VERIFICATION.md
  - docs/launch/PHASE-36-LAUNCH-READINESS.md
  - test-results/screendesign-review/**
autonomous: false
requirements: [P36-QA, P36-REMOVAL, P36-FIDELITY, P36-OPERATIONS]
must_haves:
  truths:
    - "All 47 exact 393x852 goldens, primary actions, responsive checks, a11y, and calm gates pass on the release candidate."
    - "The clean exactly-47 Plan 36-30 gallery has already passed Plan 36-29's independent validation and is not regenerated in this plan."
    - "A Vercel preview serves the intended commit SHA and local assets with no console/network errors."
    - "The user reviews the complete 47-screen comparison gallery before production merge/deploy."
  artifacts:
    - path: .planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-VERIFICATION.md
      provides: "Machine and human launch evidence"
    - path: docs/launch/PHASE-36-LAUNCH-READINESS.md
      provides: "Preview URL, commit SHA, gates, rollback, and production checklist"
  key_links:
    - from: test-results/screendesign-review
      to: 36-VERIFICATION.md
      via: "47 source/app/diff review rows"
    - from: test-results/screendesign-review/index.json
      to: test-results/screendesign-review/validation.json
      via: "Plan 36-29 independently validates the unchanged Plan 36-30 `phase36-plan30` output before this plan can start"
    - from: app/api/build-info/route.ts
      to: scripts/verify-phase36-preview-sha.ts
      via: "served VERCEL_GIT_COMMIT_SHA must equal the local release commit"
---

<objective>
Prove the release candidate in preview and obtain the only genuinely human-required approval: final visual acceptance of all 47 sources.

Purpose: Prevent production merge/deploy until automated behavior and human design intent both agree.
Output: Preview canary, complete comparison gallery, verification report, launch-readiness record, and approval checkpoint.
</objective>

<execution_context>
@C:/Users/glcar/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/glcar/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-SOURCE-COVERAGE.md
@scripts/verify-screendesign-removal.ts
@tests/screendesign-visual.spec.ts
@tests/screendesign-navigation.spec.ts
@.vercel/project.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the complete local release-candidate evidence suite</name>
  <files>.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-VERIFICATION.md, test-results/screendesign-review/**</files>
  <action>Run type, unit, tone, lint, production build, compiled removal, accessibility, responsive, and reduced-motion checks from the release commit with `SCREEN_IDS` explicitly unset. Require the unchanged Plan 36-30 `phase36-plan30` gallery and Plan 36-29 `validation.json` receipt, then rerun the validator with the same run id and `--expected-sha HEAD` without invoking the producer or mutating any source/app/diff/action artifact. Fail unless exactly 47 unique registry ids, normalized source PNGs, current app PNGs, generated diff PNGs, action/navigation JSON records, reviewed-baseline metadata, matching hashes, the current release SHA, complete status, and the matching independent receipt remain present. Missing, extra, duplicate, skipped, unreviewed, modified, manually assembled, stale, or regenerated rows fail the phase.</action>
  <verify><automated>node scripts/run-verified-commands.mjs --unset SCREEN_IDS -- npm run typecheck --next npm run test:run --next npm run tone-audit --next npm run lint --next npm run build --next npx tsx scripts/verify-screendesign-removal.ts --compiled --next npx playwright test tests/a11y.spec.ts tests/responsive-qa.spec.ts --next npx tsx scripts/validate-screendesign-review-gallery.ts --expected 47 --require-complete --expected-sha HEAD --run-id phase36-plan30 --require-file test-results/screendesign-review/validation.json</automated></verify>
  <done>The previously produced and independently validated exactly-47 evidence remains unchanged, and all repository gates pass on its release commit.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy and canary the exact Vercel preview SHA</name>
  <files>docs/launch/PHASE-36-LAUNCH-READINESS.md, .planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-VERIFICATION.md</files>
  <action>Deploy the release commit to a Vercel preview with the linked Diana project. Record preview URL, local Git commit SHA, Vercel deployment id, build status, environment-key-name presence, and rollback target without exposing values. Run `vercel inspect` as a fail-fast prerequisite, then call the read-only build-info endpoint and use `verify-phase36-preview-sha.ts` to require exact equality between the served `VERCEL_GIT_COMMIT_SHA`, the deployment inspection metadata, and `git rev-parse HEAD`. Only after equality passes, run the 47-route smoke/action subset against preview, inspect console/network failures, verify local media URLs, verify `/dashboard` Lobby composition, and run the removal canary. Do not merge or deploy production in this task.</action>
  <verify><automated>node scripts/run-verified-commands.mjs --env-copy BASE_URL=PHASE36_PREVIEW_URL -- npx vercel inspect $env:PHASE36_PREVIEW_URL --logs --next npx tsx scripts/verify-phase36-preview-sha.ts --url $env:PHASE36_PREVIEW_URL --expected HEAD --next npx playwright test tests/screendesign-navigation.spec.ts --project=screendesign-mobile</automated></verify>
  <done>The preview is healthy, deployment metadata and served build identity equal the local release SHA, local assets and route/action canary pass, and no prohibited visual system appears.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Obtain final 47-screen visual approval</name>
  <files>test-results/screendesign-review/**, docs/launch/PHASE-36-LAUNCH-READINESS.md</files>
  <action>Pause after all automated local and preview evidence is complete so the user can review the canonical source/app/diff gallery and the hosted release candidate. This checkpoint is human-required because final design intent approval cannot be inferred from automated pixel and interaction gates.</action>
  <what-built>All 47 canonical ScreenDesign states are implemented, connected, tested, and deployed to a Vercel preview. The comparison gallery contains source, app, and diff captures for every state.</what-built>
  <how-to-verify>
    1. Open the preview URL recorded in `docs/launch/PHASE-36-LAUNCH-READINESS.md`.
    2. Confirm `/dashboard` is the attached stadium/athlete Lobby, not Nexus, Mission Control, or metric cards.
    3. Review all 47 rows in `test-results/screendesign-review` and verify the app preserves each source's hierarchy, imagery, selected state, spacing, and mobile composition.
    4. Complete onboarding, open the five primary nav destinations, and spot-check assignment submit, AI policy block, public expired share, billing unavailable, and wellness save.
    5. Approve only if the preview is the intended final design; describe exact screen ids and deltas otherwise.
  </how-to-verify>
  <resume-signal>Type "approved for production merge" or list the screen ids and visual/interaction corrections required.</resume-signal>
  <verify><automated>npx tsx scripts/validate-screendesign-review-gallery.ts --expected 47 --require-complete --expected-sha HEAD --run-id phase36-plan30 --require-file test-results/screendesign-review/validation.json --require-file .planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-VERIFICATION.md --require-file docs/launch/PHASE-36-LAUNCH-READINESS.md</automated><human-check>Review the preview and all 47 source/app/diff rows using the steps above.</human-check></verify>
  <done>The user explicitly approves the release candidate for production merge, or supplies screen-specific corrections that return to the owning plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local release commit to Vercel preview | Built assets, environment configuration, and authenticated behavior cross into hosted infrastructure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36-47 | Tampering | preview SHA | high | mitigate | Record and compare Git SHA, deployment id, and served build before approval. |
| T-36-48 | Information disclosure | preview/goldens | high | mitigate | Synthetic QA data, protected preview where configured, key-name-only environment inventory. |
</threat_model>

<verification>
Require 47/47 local and preview evidence, healthy canary, exact SHA, local assets, removal audit, and explicit human design approval.
</verification>

<success_criteria>
The app is fully designed and operational in preview, all launch gates pass, and production merge/deploy is blocked only on explicit final visual approval.
</success_criteria>

<output>
Create `.planning/phases/36-faithfully-implement-all-47-figma-html-screens-and-remove-th/36-24-SUMMARY.md` when done.
</output>
