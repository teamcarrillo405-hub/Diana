---
phase: 36-faithfully-implement-all-47-figma-html-screens-and-remove-th
plan: "32"
type: execute
wave: 10
depends_on: ["36-31"]
gap_closure: true
files_modified:
  - app/public-home-funnel.tsx
  - app/onboarding/screendesign-onboarding.tsx
  - app/(app)/upgrade/upgrade-screen.tsx
  - app/screendesign.css
  - tests/public-home-funnel.spec.ts
  - tests/screendesign-navigation.spec.ts
autonomous: true
requirements: [P36-FIDELITY, P36-OPERATIONS, P36-QA]
must_haves:
  truths:
    - "The public root contains all six attached compositions at the same time in one vertical document."
    - "Forward and back controls scroll between adjacent compositions without replacing the current screen or persisting a stage."
    - "Quiz selections remain interactive and the validated draft still reaches signup."
    - "Authenticated onboarding and upgrade routes keep their existing single-screen behavior."
    - "Nexus, Mission Control, command-deck, and game-plan implementation language remains absent."
  artifacts:
    - path: app/public-home-funnel.tsx
      provides: "One continuous public ScreenDesign document"
    - path: tests/public-home-funnel.spec.ts
      provides: "Browser proof that all sections coexist and use document scrolling"
  key_links:
    - from: app/public-home-funnel.tsx
      to: app/onboarding/screendesign-onboarding.tsx
      via: "scroll presentation renders all four onboarding compositions concurrently"
    - from: app/public-home-funnel.tsx
      to: app/(app)/upgrade/upgrade-screen.tsx
      via: "both truthful paywall compositions are sibling sections in the same document"
---

<objective>
Correct the public home interpretation from a screen-replacement funnel to one continuous vertical ScreenDesign experience.

Purpose: The user intended the attached HTML compositions to flow downward with browser scrolling. The state-machine implementation hid every composition except the current one and could reopen on a later screen.
Output: A six-section public page with smooth adjacent navigation, a single document scrollbar, preserved quiz persistence, and no Nexus carryover.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render all attached public compositions in document order</name>
  <files>app/public-home-funnel.tsx, app/onboarding/screendesign-onboarding.tsx, app/(app)/upgrade/upgrade-screen.tsx, app/screendesign.css</files>
  <behavior>
    - Welcome, educational, challenge, schedule, community access, and standard access sections exist in the DOM immediately after hydration.
    - Primary and back controls use reduced-motion-aware scrollIntoView navigation.
    - The public page has one main landmark and uniquely labelled section landmarks.
    - Schedule completion writes the same validated session draft before scrolling onward.
    - Only the final standard-access action navigates to signup.
  </behavior>
  <action>Add a scroll presentation seam to the existing ScreenDesign onboarding component and a public-scroll seam to UpgradeScreen. Keep authenticated wizard and upgrade behavior unchanged. Replace the public stage state machine with a single ordered main document. Remove internal section scroll traps in public mode so the browser owns the vertical flow.</action>
  <verify><automated>node scripts/run-verified-commands.mjs -- npx playwright test tests/public-home-funnel.spec.ts --project=chromium --workers=1 --next npm run typecheck --next npm run tone-audit</automated></verify>
  <done>Every attached public composition is present, ordered, scrollable, interactive, and no public stage can hide the rest of the page.</done>
</task>

<task type="auto">
  <name>Task 2: Rebind navigation and release gates to the scrolling contract</name>
  <files>tests/public-home-funnel.spec.ts, tests/screendesign-navigation.spec.ts</files>
  <action>Replace state-replacement assertions with DOM-order, scroll-position, answer-persistence, draft, and final-signup assertions. Run the canonical 47-screen no-update visual suite, accessibility/responsive suite, unit tests, build, tone, lint, and obsolete-design removal checks.</action>
  <verify><automated>npx playwright test tests/public-home-funnel.spec.ts --project=chromium --workers=1; $env:SCREEN_IDS='onboarding-welcome,onboarding-educational,onboarding-quiz-challenge,onboarding-quiz-schedule,paywall-social-proof,paywall-standard'; npx playwright test tests/screendesign-navigation.spec.ts --project=screendesign-mobile --workers=1; Remove-Item Env:SCREEN_IDS; npx playwright test tests/screendesign-visual.spec.ts --project=screendesign-mobile --workers=1; $env:QA_BASE_URL='http://127.0.0.1:3005'; $env:QA_CREATE_USER='true'; npx playwright test tests/a11y.spec.ts tests/responsive-qa.spec.ts --project=chromium --workers=1; npm run test:run; npm run typecheck; npm run lint; npm run tone-audit; npm run build; npx tsx scripts/verify-screendesign-removal.ts --source; npx tsx scripts/verify-screendesign-removal.ts --compiled</automated></verify>
  <done>The continuous public page and all 47 canonical routes pass release gates without visual baseline updates or Nexus restoration.</done>
</task>

</tasks>

<success_criteria>
- One browser scrollbar reveals all six attached compositions from top to bottom.
- No reload or session state can reopen the root on a hidden later stage.
- Quiz answers and signup metadata behavior remain operational.
- Authenticated screens and all 47 canonical sources remain unchanged.
</success_criteria>
